# 「도토리숲 대모험」 인수인계서 v4 (세션 2 이어서 · 무인 진행분)

작성 2026-08-30 (같은 날, v3 작성 이후) · v1(HANDOVER.md)·v2(세션로그)·v3(HANDOVER_v3_session2.md)를
대체하지 않고 이어붙인다. 사용자가 "할 수 있는 잔여 작업 모두 절대규칙 지키면서 진행해줘, 나 자고
올거다"라고 지시해 이 문서 작성 시점까지 감독 없이 진행했다. **결론부터: v3가 "다음 세션 최우선"으로
남긴 자세 그림 배선을 실제로 마쳤지만, 배선한 결과를 실측해보니 자세 그림 자체가 목적을 달성하지
못해 기능을 꺼 둔 채로 남겼다. 아래 3절을 반드시 읽고 진행할 것.**

---

## 1. 이번 세션(무인 진행분) 요약

| # | 한 일 | 상태 |
|---|---|---|
| 1 | v3가 미룬 move/atk 자세 시트 2장 추가 생성 (Gemini) | ✅ 완료 |
| 2 | game.html에 자세 스프라이트 배선 (wepGrip·heroFist·drawHandOver·drawHero·heroDollCanvas 5곳) | ✅ 완료·코드 검증됨 |
| 3 | 배선 결과를 t_audit3(감사)로 실측 — **자세 그림 자체가 문제 있음을 발견** | ⚠ 발견 |
| 4 | 원인 특정(그림에서 손이 뺨 옆에 그대로 붙어 있음) + 성능 회귀 여부 대조 검증 | ✅ 완료 |
| 5 | 판단: 배선 코드는 남기고 실제 사용(WEP_POSE_CLASS)만 꺼서 회귀 0건으로 되돌림 | ✅ 완료 |
| 6 | 커밋 (a45aa2b) | ✅ 완료 |

## 2. 배선 자체는 정확하다 (코드 검증 완료)

v3가 설계해 둔 5곳을 그대로 구현했다:
- `wepGrip(id)` — 자세 그림이 켜져 있으면(`HERO_ART_KEY`) `POSE_ANG+WEP_RULE` 계산 대신
  `HERO_POSE[...].ang`을 목표각으로 직접 쓴다.
- `heroFist()` — 자세 그림 프레임이면 `HERO_POSE`의 쥐는점(hx,hy,d)을 바로 반환한다.
- `drawHandOver()` — 자세 그림 전용 주먹 사각형(fx0~fy1)으로 다시 그리는 경로를 추가했다.
- `drawHero()` — idle/move/atk 상태에 맞춰 몸 자체를 자세 스프라이트로 교체한다.
  **여기서 실제로 버그 하나를 미리 잡았다** — 위로 걸을 때 뒷모습(`heroBackSprite()`)으로
  바뀌는 기존 기능이 있는데, 자세 그림은 정면만 있어서 뒷모습일 때 자세를 켜 두면 무기가
  뒷모습 실루엣과 어긋난 자리에 붙는다. `_isBack`일 때는 자세를 강제로 끄도록 안전장치를 넣었다.
- `heroDollCanvas()` (장비창 미리보기) — 직전 실제 게임 프레임의 자세 상태가 새어 들어오지
  않도록 함수 시작에서 자세 상태를 매번 리셋한다.

검증: `t_regress.js` 25/25, `t_room3.js` 81/81 — 배선 코드 자체는 기존 기능을 전혀 건드리지 않았다.

## 3. ⚠ 그런데 실측해보니 자세 그림이 문제를 해결하지 못했다 (핵심 발견)

### 3-1. 어떻게 발견했나
배선 직후 `tests/t_audit3.js`(무기가 얼굴·몸통을 가로지르는 비율을 픽셀 단위로 재는 감사 스크립트)를
돌렸는데, 결과가 무기를 갈아 낄 때마다 들쭉날쭉했다. 원인 추적: `T.beginPlay()`로 실제 게임을
띄우면 근처 몬스터와 자동으로 전투가 붙어 `P.atkT`(공격 중)가 수시로 켜지는데, 자세 그림 배선
이후엔 idle과 atk가 서로 다른 자루각을 쓰므로 **감사를 돌릴 때마다 우연히 idle이었는지 atk였는지에
따라 결과가 달라졌다.** 이건 감사 스크립트 자체의 결함이라 판단해 먼저 고쳤다 — 무기를 바꿀 때마다
몬스터를 비우고(`EN.length=0`) `atkT`·`moving`을 강제로 0으로 만든 뒤 애니메이션 프레임 2번을
기다려 idle 상태를 고정시켰다(재현 가능하게).

### 3-2. 고친 감사로 다시 재니 — 장병기 6종 전부 악화
| 무기 | 배선 전(숫자 전용) 얼굴% | 배선 후(자세 그림) 얼굴% |
|---|---|---|
| 창(spear) | 2.7% | **47.1%** |
| 낫(scythe) | 14.4% | 31.5% |
| 미늘창(halberd) | 6.7% | 33.6% |
| 삼지창(trident) | 8.0% | 34.0% |
| 언월도(glaive) | **0%(깨끗)** | 20.7% |
| 사슬낫(kusari) | 8.8% | 16.5% |

**6종 전부 배선 전보다 나빠졌다** — 특히 언월도는 원래 유일하게 깨끗했는데 오히려 망가졌다.

### 3-3. 원인을 그림에서 직접 확인함 (추측 아님)
`art/hero_pose_pole.webp`를 직접 열어봤다. 의도는 "팔을 몸 밖으로 뻗은 자세"였지만, 실제로는
든 손이 **뺨 옆(hy=-0.5322 — 어깨보다도 높은, 머리 높이)에 그대로 붙어 있다.** 게다가 봉의 각도도
대각선(ang=-0.4743rad ≈ -27°)이다. 반면 옛 숫자 전용 방식(`WEP_RULE.pole = [0.0, 1.55, 1]`)은
기울기 보정값이 0으로, **거의 수직으로 세운 자세**를 쓰도록 이미 튜닝돼 있었다(a6714a7 커밋에서
pole/staff 분리할 때 얼굴 겹침을 줄이려고 일부러 그렇게 잡은 값). 즉 새 자세 그림은 "손이 몸 밖으로
나갔다"는 원래 의도를 그림 단계에서 달성하지 못했고, 거기에 대각선 각도까지 더해져 오히려 옛
방식보다 얼굴을 더 많이 가로지르게 됐다.

### 3-4. 성능 회귀는 아님을 확인함 (혹시나 해서 대조 검증)
장병기 실측과 별개로, 이 배선이 프레임 성능에 영향을 줬는지도 `t_galaxy.js`(갤럭시 9개 기종
CPU 스로틀링 시뮬레이션)로 대조했다. 배선 켠 상태와, `git stash`로 배선 이전 커밋(84d15cc)으로
되돌린 상태 둘 다 돌려봤는데 **결과가 완전히 같다**(갤A16/A15·갤탭A9+·갤S8 3개 기종만 ⚠, 나머지
6개는 ✅ — 수치도 거의 동일). 이 ⚠는 이 세션 이전부터 있던 현상이고 배선과 무관하다는 뜻이다.

### 3-5. 내린 판단
배선 코드(위 2절)는 정확하고 안전하다 — 문제는 순전히 "자세 그림 자체의 손 위치·봉 각도"다.
그래서:
- **`WEP_POSE_CLASS`를 빈 객체(`{}`)로 둬서 실제 게임은 계속 예전 방식(HERO_GRIP/WEP_RULE)으로
  동작하게 했다.** `poseKeyFor()`가 항상 null을 돌려주므로 새 코드는 완전히 비활성 상태다.
- 배선 코드·`HERO_POSE`·시트 파일(`hero_pose_pole*.webp`)은 전부 그대로 남겨 뒀다 — 그림만
  다시 만들면 `WEP_POSE_CLASS={pole:'pole'}`로 한 줄 되돌리기만 하면 된다.
- 원인과 재생성 지침을 `game.html`의 `WEP_POSE_CLASS` 선언부 바로 위 주석에 상세히 남겼다.

검증(최종): `t_regress.js` 25/25, `t_room3.js` 81/81, `t_audit3.js` 17/48(배선 이전과 완전히
동일한 숫자 — 회귀 0건 확인).

## 4. 다음 세션이 자세 그림을 다시 만들 때 지켜야 할 것

1. **쥐는 손을 뺨이 아니라 가슴~허리 높이까지 확실히 내려 뻗는다.** 기준: `hy`가 최소 -0.30
   이하로 내려가야 얼굴 상자(FY0=8%~FY1=46%, 몸 bbox 기준) 밖으로 나간다. (이번 시트의
   hy=-0.5322는 머리 높이라 실패했다.)
2. **봉 각도를 대각선이 아니라 거의 수직으로 세운다.** 옛 `WEP_RULE.pole[0]=0.0`처럼, 반대쪽
   어깨나 얼굴을 가로지르지 않는 각도로 프롬프트/구도를 다시 잡는다.
3. **재생성 뒤 반드시 `node tests/t_audit3.js`로 먼저 수치 확인** — idle 상태를 자동으로 고정하도록
   이미 고쳐져 있으므로 무기를 바꿔가며 재현 가능하게 측정된다. 배선 전(`WEP_POSE_CLASS={}`)
   숫자보다 얼굴·몸통 겹침이 실제로 줄었는지 반드시 대조한다.
4. 수치가 통과해도 **스크린샷으로 눈으로 반드시 확인한다** (절대규칙 5). 이번 세션에서 실제로
   그림을 열어보고 나서야 문제를 확정했다 — 수치 왜곡 가능성(감사 스크립트의 얼굴 상자·손
   반지름 근사치 오차)이 있으므로 시각 확인이 최종 판정이다.
5. 두 가지 다 통과하면 `var WEP_POSE_CLASS={};`를 `var WEP_POSE_CLASS={pole:'pole'};`로
   되돌리고, 갑옷 낀 상태·다른 계열(heavy/bow/item)로 확장을 검토한다.

## 5. 다음 세션 우선순위 (v3의 5절 갱신판)

1. **[최우선]** 4절 지침대로 pole 자세 그림 재생성 → 검증 통과하면 `WEP_POSE_CLASS` 재활성화
2. (재활성화 성공 시) 계열별 자세를 heavy/bow-item까지 확장, 갑옷 6종에도 적용할지 결정
3. B. 방향별 그리는 순서표 (v1에서 미착수)
4. C. 머리 장비(투구) 표시 (`HEAD_OVERLAY=0`, v1에서 미착수)
5. D. UI 화면 4개 정비, E. 펫 시스템, F. 구글 플레이 출시물 (전부 미착수) — **최종 목표가 구글
   플레이 판매(6~10세, 3,300원)임을 항상 기억할 것.**

## 6. 검증 방법 (v1·v3와 동일 + 이번에 추가된 것)

```bash
cd /root/toriforest && source env.sh
python3 build.py
node t_regress.js         # 25/25
node t_room3.js            # 81/81
node t_nan.js
node t_galaxy.js           # 9개 갤럭시 기종 대조 (2~5분 걸림, 백그라운드 권장)
node tests/t_audit3.js     # 무기별 얼굴·몸통 겹침 실측 (이번 세션에 idle 고정으로 수정함)
python3 audit_mon.py
```

이번 세션(무인 진행분) 끝 시점 확인: `t_regress` 25/25 · `t_room3` 81/81 · `t_audit3` 17/48(배선
전과 동일) · `t_galaxy` 문제 기기 3/9(배선 전과 동일, 회귀 아님). 빌드 정상(521 그림·12.05MB).
`WEP_POSE_CLASS={}`이므로 게임 실제 동작은 이 세션 시작 시점과 완전히 같다(자세 그림은 코드만
대기 중, 실사용 없음).
