# 「도토리숲 대모험」 인수인계서 v3 (세션 2)

작성 2026-08-30 · 이 문서는 v1(HANDOVER.md)·v2(세션로그-환경복구, claude.ai 프로젝트에 있음)를
대체하지 않고 이어붙인다. **v1의 절대규칙·아키텍처 설명(마젠타 봉 기법 등)을 먼저 읽을 것.**
이번 세션은 v1의 "4-3 남은 작업 A(무기 계열별 자세)"부터 시작해, 도중에 사용자가 추가 지시한
던전 구조 개편·무기 계열 재분류·전투 모션 벤치마킹까지 진행했다.

---

## 0. 이번 세션 작업 순서 요약

| # | 한 일 | 상태 |
|---|---|---|
| 1 | 도달불가 코드 2곳 조사 (spreadCamps_DISABLED, doUlt) — 둘 다 의도된 것 확인 | ✅ 완료 |
| 2 | 던전 구조를 던파식 "방 3개 + 순차 게이트"로 개편 | ✅ 완료·검증됨 (커밋 a6714a7) |
| 3 | 무기 계열 재분류 (pole → pole+staff 분리, 18→17종 개선) | ✅ 완료·검증됨 (커밋 a6714a7) |
| 4 | Chrome 확장 프로그램 연결 (사용자가 재설치/재연결함) | ✅ 완료 |
| 5 | 장병기 자세 시트 1차 생성 + `slice_pose.py` 실제 버그 수정 | ✅ 완료 (커밋 37e35b9) |
| 6 | 전투(일반공격·스킬) 모션을 DNF·커비·붉은보석과 대조 벤치마킹 | ✅ 완료 (문서만, 코드 변경 없음) |
| 7 | game.html에 자세 스프라이트 배선 | ⏸ **보류** (이유는 3절) |

---

## 1. 던전 3방 구조 (던파식) — 완료

각 스테이지를 "방 3개 + 게이트"로 나눴다. 기존 게임의 실제 챕터 전환 방식이 **물리적 충돌이 아니라
`exitGate.open` 불리언 플래그 기반**이라는 걸 먼저 확인했고, 그 위에 gate1/gate2 두 개를 똑같은
방식으로 추가하는 게 가장 위험이 낮다고 판단해 그렇게 구현했다.

- `chapRoomNeed(c, room)` : 방별 처치 요구량 (전체의 34%/67%/100%)
- `WD.gate1`/`WD.gate2` : 기존 exitGate와 같은 패턴(`.open`/`.solid`), `applyProgress()`에서 계산
- `updateCamps()` : 잠긴 방의 몹 스폰 억제
- 미니맵에 게이트 아이콘 + "잠김" 라벨
- **실제로 발견해서 고친 버그**: `spreadCamps()`라는 기존 코드가 챕터당 몹 캠프를 3개가 아니라
  9개(보너스 캠프 6개 추가)로 만들고 있었다 — 방 잠금과 무관하게 잠긴 방에도 몹이 나오는 버그였다.
  좌표 덤프로 직접 발견했고, `spreadCamps_DISABLED`로 비활성화했다(방 잠금 설계와 근본적으로
  안 맞는 기능이라 판단, 숫자만 고치는 미봉책 대신 기능을 껐다).
- 검증: `t_room3.js` 81/81 (8개 지형 타입 × 9개 대표 챕터), `t_regress.js` 25/25, 스크린샷 확인함

## 2. 무기 계열 재분류 — 완료 (부분적)

`WEP_CLASS`에서 `staff`/`rune`/`starst`/`broom`/`fishrod`/`brush`/`umbrella` 7종을 기존 `pole`에서
분리해 새 `staff` 계열로 만들고 `WEP_RULE.staff = [-0.18, 1.30, 0.55]`를 추가했다. 무기 기하 감사
(`t_audit3.js`)에서 얼굴/몸통 겹침 문제가 18종 → 17종으로 줄었다.

**이건 진짜 해결이 아니라 임시 완화다** — 근본 원인(모든 무기가 자세 하나를 숫자로만 돌려 쓰는 구조)은
그대로다. 진짜 해결은 3절.

## 3. 무기 계열별 자세 그림 — 1차 생성, 배선은 보류

### 3-1. Chrome 확장 연결 문제 해결됨
세션 초반 `list_connected_browsers`가 빈 배열을 반환해 Chrome 확장이 연결 안 된 상태였다. 사용자가
직접 Chrome에서 확장 설치/로그인/연결을 마쳤고, 이후 정상 연결됨을 확인했다(`select_browser` 성공,
Gemini에 "파파5님"으로 이미 로그인돼 있었음 — 원래 문서화된 파이프라인 그대로 사용 가능).

### 3-2. 장병기(pole) 자세 시트 생성 — 성공
`슬라이서(slice_pose.py)`로 처리 가능한 시트를 Gemini로 생성했다. **핵심 발견**: 두 손이 봉을 양쪽
끝에서 각각 쥐는 구도(처음 시도)는 실패했다 — 봉이 몸통을 대각선으로 가로지르면서 털색↔배색 경계를
지나가는데, 슬라이서의 구멍 채우기 알고리즘이 그 경계를 처리 못 해 가슴에 흰 얼룩이 남았다(94px,
눈으로 보면 뚜렷한 흰 사각형). **원인을 픽셀 단위로 추적**해서 확인했다(`walk()` 함수가 봉 양쪽에서
색이 크게 다르면 아예 채우기를 포기하던 코드).

**두 가지로 고쳤다:**
1. **구도를 바꿨다** — 한 손(정면 기준 오른쪽 앞발)만 봉을 쥐고, 반대쪽 손은 봉과 무관하게 몸에
   붙이는 구도로 재생성. 팔이 몸 밖으로 확실히 뻗어 있으면서도 봉이 몸통을 가로지르지 않는다.
2. **`slice_pose.py` 자체를 고쳤다** — 봉 양쪽 색이 갈릴 때(색 경계를 봉이 지나갈 때) 무조건
   포기하던 것을, 색이 가까우면 기존처럼 섞고 색이 갈리면 **더 가까운 쪽 색을 그대로 쓰도록** 바꿨다
   (양쪽 색을 섞으면 존재하지 않는 중간색이 되어 더 눈에 띈다). 이 수정은 **일반적인 개선**이라
   앞으로 만들 모든 자세 시트에 재발하지 않는다.

결과: `art/hero_pose_pole.webp` (맨몸, 장병기 계열 팔 뻗은 정지 자세), `art/hero_pose.json`에 쥐는점
(hx,hy) · 자루각(ang) · 주먹 사각형(fx0~fy1) 저장됨. 흰 얼룩 94px → 순수백 21px(눈 하이라이트
수준, 문제 없음)로 해소 확인.

원본 소스 이미지는 `srcart/hero_pose_pole_src.png`에 보관(재슬라이스나 재검증 시 필요).

### 3-3. ⚠ game.html 배선은 하지 않았다 — 이유
`hero_pose_pole.webp`는 **정지 자세 한 장뿐이다.** 현재 캐릭터는 idle/move/atk/hurt 등 여러 그림을
번갈아 그리는데(`HERO_FIST` 표에 `hero_idle`/`hero_move`/`hero_atk`/`hero_hurt`/`hero_inhale`/
`hero_ko` 각각 다른 주먹 좌표가 있음 — 애니메이션 프레임마다 그림 자체가 다르다는 뜻이다), 정지
자세 한 장만 배선하면 **다른 프레임으로 넘어가는 순간 무기 파지가 튄다**(깜빡이거나 순간적으로
잘못된 위치에 무기가 붙는다). 이건 배선 전에 반드시 idle·move·atk 최소 3장(이상적으로는 갑옷
6종 각각)이 갖춰져야 한다는 뜻이라, **이번 세션에서는 배선하지 않기로 판단했다.**

**다음 세션이 이어받을 것:**
1. `art/srcart/hero_pose_pole_src.png`를 참고해 같은 스타일로 `hero_pose_pole`의 move/atk 버전을
   추가 생성 (같은 프롬프트에 "walking" / "mid-swing" 자세만 바꿔서)
2. 계열을 3~4개로 압축하는 게 안전하다는 결론(v1 문서 참고)에 따라 pole+staff 계열 다음으로
   heavy(양손 대형무기), bow/item(원거리·소품) 계열도 같은 방식으로 생성
3. 갑옷 6종에도 같은 자세를 적용할지, 맨몸에만 적용하고 갑옷은 기존 방식을 유지할지 결정 필요
   (전자가 이상적이지만 물량이 6배— 계열 수 × 갑옷 수 × 프레임 수)
4. `wepGrip()`이 `WEP_CLASS`의 계열에 따라 `hero_pose_*` 스프라이트 + `hero_pose.json`의 자체
   `hx/hy/ang`을 직접 쓰도록 배선 (기존 `POSE_ANG + WEP_RULE` 계산 경로를 대체)

## 4. 전투 모션 벤치마킹 — 문서만, 코드 변경 없음

사용자 지시("일반 공격, 스킬 시전 모션도 위 3가지 게임을 정밀 분석·벤치마킹")에 따라 실제 웹 검색으로
DNF 공식 가이드·나무위키·붉은보석 리뷰 기사를 조사하고, 현재 `game.html`의 전투 코드
(`castAbil`/`castSkillCore`/`skillCastFX`/`sigFor`/`skillColor`)를 직접 읽어 대조했다.
전체 내용은 `DNF-Kirby-BurningStone-전투벤치마킹.md`에 있다 (claude.ai 프로젝트에도 저장 예정).

**핵심 결론만 요약**: 입력 버퍼링("죽는 입력 없음", DNF 스타일)·스킬별 색상 90도 분리·이펙트
다양화는 **이미 벤치마크 수준으로 구현돼 있었다.** 실제로 비어 있던 것은 **스킬마다 캐릭터 몸
자체의 포즈가 다르지 않다**는 것뿐이었고, 이는 3절의 무기 계열별 자세 작업과 정확히 같은 작업이다
— 별도 시스템이 필요한 게 아니라, 진행 중이던 자세 시트 작업을 스킬 종류별로 넓히면 된다.

## 5. 다음 세션 우선순위 (v1의 "4-3 남은 작업" 갱신판)

1. **[최우선]** 3-3절의 배선 전 준비 (move/atk 자세 추가 생성) → 완료되면 배선
2. 계열별 자세를 heavy/bow-item까지 확장
3. B. 방향별 그리는 순서표 (v1에서 미착수)
4. C. 머리 장비(투구) 표시 (`HEAD_OVERLAY=0`, v1에서 미착수)
5. D. UI 화면 4개 정비, E. 펫 시스템, F. 구글 플레이 출시물 (전부 미착수)

## 6. 검증 방법 (변경 없음, v1과 동일)

```bash
cd /root/toriforest && source env.sh
python3 build.py
node t_regress.js      # 25/25
node t_room3.js         # 81/81 (이번 세션에 추가된 던전 3방 검증)
node t_nan.js
node t_galaxy.js
python3 audit_mon.py
```

이번 세션 끝 시점 확인: `t_regress.js` 25/25, 빌드 정상(519 그림·0.30MB 글꼴·12.0MB 산출물).
`hero_pose_pole.webp`는 배선 안 했으므로 게임 동작에 아무 영향 없음(회귀 영향 없음 확인됨).
