# 「도토리숲 대모험」 인수인계서

작성 2026-08-30 · 작성자 Claude (Opus 5)
다음 담당자에게 그대로 넘긴다. **먼저 「0. 절대규칙」을 읽고 시작할 것.**

---

# 0. 절대규칙 (사장님 지시 · 최우선 · 예외 없음)

1. **모든 작업은 초고퀄리티·초고도화로 정밀하게 진행**
2. **오류 절대 발생 금지**
3. **수정 → 확인 → 검증 → 재검증 단계를 반드시 거칠 것**
4. **UI·해상도 등 모든 것이 최적화·시인성·직관성 우수할 것**
5. **벤치마킹 가능한 인터넷 자료를 검색·수집·분석·확인·적용·검증·재검증**
6. **추측에 의한 작업 절대 금지 — 사실·데이터 근거가 있는 작업만**
7. **매 작업 시 타 게임 상위 20개를 조사, 비교 분석, 정확·정밀하게 벤치마킹**
8. **(2026-08-31 추가) 장비를 캐릭터에 "덧붙이는" 문제는 좌표 계산으로 때우지 말고,
   제미나이(Ultra, 일일 한도 사실상 없음)로 "이미 착용한 캐릭터 전신"을 새로 그려서
   푼다.** 기존 아이템 칸 아이콘(eq_*)을 좌표·스케일 계산만으로 몸 위에 겹치는 방식은
   구조적으로 한계가 있다 — 아이콘 자체가 "착용 전용 구도"로 그려진 게 아니라서, 아무리
   좌표를 정밀하게 재도 모자(cap)류는 안 보이거나 투구(helm)류가 머리와 떠 보이는 등
   완전히 자연스럽게는 못 붙는다(2026-08-31 실측으로 확인, mkwear.py 방식 폐기 검토).
   무기 자세 그림(hero_pose_pole 등)을 마젠타 봉 기법으로 다시 그려 해결했던 것과
   똑같은 원리로, 장비 조합마다 "그 조합을 완전히 입은 캐릭터 전신"을 AI로 새로 그리고
   실측·스크린샷으로 검증한 뒤 그 결과물 통째로 쓴다. 이 방식이 항상 이번 절대규칙
   1번(초고퀄리티)·6번(추측 금지)에 더 부합한다.

## 0-1. 추가 상시 제약

- **갤럭시 탭 A9+ 에서 쾌적하게 작동** (끊김·멈춤·강제종료 없을 것)
- **갤럭시 스마트폰들에서도 잘 돌아가야 한다** (보급형 A 시리즈 포함)
- **게임 내 모든 이미지는 AI 생성이어야 한다.** AI 생성이 아닌 게 게임 안에 하나도 없어야 한다
- 게임 용량 300MB 이상 되어도 됨 (현재 APK 11MB)
- **저작권** : 오리지널 캐릭터만. 레퍼런스는 디자인 언어 학습용으로만 본다
- **6~9살 아이들이 극도로 좋아할 게임**을 만들 것. 관련 자료를 모조리 검색·조사할 것
- 무기 잡은 모습·착용한 모습·휘두르는 모습·공격 모습 전부 **타 2D게임 상위 20개와 동급 퀄리티**
- **모든 무기 쥐었을 때 이상하면 절대 안 됨**
- 최종 목표 : **구글 플레이 출시** (6~10세, 3,300원)

## 0-2. 일하는 방식 (사장님이 반복해서 요구한 것)

- **"하나 고치면 하나 터지는" 것을 멈춰라.** 증상이 아니라 **원리**를 고쳐라
- 눈대중 금지. **실제 파일을 픽셀로 재고, 렌더해서 눈으로 확인**한 뒤에 말할 것
- 모르면 모른다고 하고, 틀렸으면 틀렸다고 할 것

---

# 1. 프로젝트 개요

| 항목 | 값 |
|---|---|
| 게임 | 「도토리숲 대모험」 쿼터뷰 육성 RPG |
| 대상 | 사장님 아들(7세), 구글 플레이 6~10세 |
| 주인공 | 다람쥐 (주황 털 #E8873C, 크림 얼굴·배 #F6E3C0, 초록 목수건, 외곽선 #3A2418) |
| 원본 | `/root/toriforest/game.html` (약 1.26MB, 단일 HTML) |
| 빌드 | `python3 build.py` → `dotorisup.html` (art/*.webp + 글꼴을 base64로 주입, 12.5MB) |
| APK | `cd /root/apk && bash build.sh` → `도토리숲 대모험.apk` (11.1MB) |
| 형상관리 | `/root/toriforest` 가 git 저장소. 커밋 메시지에 **원인과 근거**를 적을 것 |

### 콘텐츠 규모
- 지역 20 · 장 60
- 몬스터 **240종** (지역당 12 : 일반 8 · 정예 3 · 보스 1)
- 무기 48종류 / 장비 아이템 220종 · 방어구 110종
- 재료 72종 (12계열 × 6형태) · 소품 33종
- 능력 30종 × 3단계

---

# 2. ★ 핵심 아키텍처 — 무기·방어구 착용 원리

> 이게 이 프로젝트에서 가장 오래 헤맨 부분이다. **원리를 이해하지 못하면 또 헤맨다.**

## 2-1. 어떤 무기·방어구를 껴도 안 깨지려면 (상용 엔진 문서 조사 결론)

| | 원리 | 상태 |
|---|---|---|
| ① | **캐릭터 그림마다 '쥐는 자리'가 숫자로 선언돼 있어야 한다** (좌표·자루 각도·굵기) | ✅ |
| ② | **무기 그림마다 '손잡이 자리'가 같은 규격으로 선언돼 있어야 한다** | ✅ |
| ③ | **그리는 순서가 규칙이어야 한다** (몸 → 무기 → 앞손) | ✅ |
| ④ | **자세는 무기 '계열'이 고른다** (계열은 유한, 계열마다 ①을 따로 가짐) | ⏳ 미완 |

근거 : Spine(뼈·슬롯·스킨) · Cocos Creator(Socket) · Unity(BoneFollower / Sprite Library) ·
Godot(컷아웃) · Live2D(재료 분리) · Adobe(레이어 분리) · Mana Seed(동일 배치 규칙).
**던전앤파이터도 같은 구조다** — 자세한 것은 프로젝트 문서 `던전앤파이터-방식-조사.md` 참고.

## 2-2. 마젠타 봉(magenta rod) 기법 — 이 프로젝트의 핵심 도구

AI에게 캐릭터를 그리게 할 때 **손에 납작한 마젠타(#FF00FF) 봉을 쥐게 한다.**
그 봉을 지우면 **쥐는 점 · 자루 각도 · 자루 굵기 · 주먹 사각형**이 그림에서 그대로 나온다.
**추측이 개입할 여지가 구조적으로 없다.**

- 슬라이서 : `slice_hero.py` (갑옷 전신) · `slice_pose.py` (계열별 자세) · `slice_paw.py` (손만)
- 결과 : `art/hero_grip.json` (갑옷 6종 + 맨몸의 쥐는 점·주먹 사각형)

### ⚠ 반드시 지킬 프롬프트 규칙
> **"봉은 외곽선 없이 납작한 마젠타만 (flat magenta, NO outline)"**

AI가 봉에 **어두운 외곽선**을 그리면 색으로는 절대 못 지운다. 검은 대각선이 남는다.
(자세 시트 3종이 이것 때문에 아직 게임에 못 들어갔다.)

## 2-3. 무기 손잡이 자동 검출 — `gripsolve.py`

**예전엔 무기 48종의 좌표를 손으로 박아 넣었다(48개의 예외). 그래서 하나 고치면 하나가 틀어졌다.**
지금은 그림에서 직접 잰다:

1. 실루엣의 **주축**을 주성분분석(PCA)으로 구한다 → 자루 방향
2. 주축을 따라 **두께 단면도(20칸)** 를 낸다
3. **손잡이 판정** : "얇은 쪽"이 아니라 **"덜 뾰족한 쪽"**이 손잡이다
   - ⚠ **칼끝도 얇다.** '얇은 쪽=손잡이'로 하면 칼날을 잡고 거꾸로 든다 (실제로 그랬다)
   - 판정식 : `맨끝 굵기 / 안쪽 3칸 평균` 이 큰 쪽이 손잡이 (손잡이는 굵기가 일정 + 폼멜)
4. 쥐는 점 = 손잡이 끝에서 길이의 **0.263**(검 계열) 또는 **0.409**(장병기) — 레퍼런스 실측값
5. 양 끝 두께가 비슷하고 뭉툭하면 손잡이 없는 물건 → **무게중심**을 쥔다

출력 : `art/wep_grip.json` (67종). **무기를 새로 넣으면 좌표가 저절로 나온다.**

## 2-4. 사람이 정하는 것은 '계열' 한 단어뿐

`game.html` 의 `WEP_CLASS` : 무기 48종 → 계열 7종 (small/blade/heavy/blunt/pole/bow/item)
`WEP_RULE` : 계열 → `[기울기(수직 기준 라디안), 크기배수, 손높이]`

`wepGrip()` 은 이제 **두 좌표계를 겹치기만 한다** :
```
그림이 알려 준 자루 방향(A.ang)  →  자세가 요구하는 방향(POSE_ANG + 계열 기울기)
```

## 2-5. 앞손(hand_front)

**딴 손 그림을 얹지 않는다.** 캐릭터 그림에 이미 그려진 **주먹 사각형만 무기 위에 다시 그린다.**
(`drawHandOver` → `HERO_GRIP[key].fx0/fx1/fy0/fy1` 로 클립하고 몸을 한 번 더 그린다)
화풍·색·크기가 태생부터 맞는다. Spine 의 hand_front 슬롯을 그림 자체에서 뽑는 셈이다.

⚠ **가로 폭을 주먹보다 넓히면 꼬리까지 다시 그려져 무기를 가린다.** (실제로 가렸다)

---

# 3. 그림 제작 파이프라인 (AI 생성)

## 3-1. 경로 : Gemini → 클립보드 → 컨테이너

크롬이 gemini.google.com 다운로드를 막아서 **클립보드 우회**를 쓴다.

```
① 크롬(사장님 PC)에서 Gemini 로 시트 생성
② 이미지의 [이미지 복사] 버튼 클릭
③ powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\Downloads\clipsave.ps1" <이름>
   → C:\Users\sun\Downloads\<이름>.png 로 저장
④ device_stage_files 로 컨테이너에 올림
⑤ 슬라이서로 잘라 art/ 에 저장
⑥ python3 build.py
```

`clipsave.ps1` 은 이미 사장님 PC의 Downloads 에 있다.

### 브라우저 조작 요령
- 새 대화 탭을 만들고 `find` 로 입력창 ref 를 잡아 `type` → 전송 버튼 클릭
- 전송 후 **40초** 기다린 뒤 아래로 스크롤 → 이미지 위에 hover → 좌표 **(1089, 293)** 이 복사 버튼
- 탭이 길어지면 렌더러가 멈춘다. **새 탭으로 옮길 것**

## 3-2. 슬라이서

| 파일 | 용도 | 특징 |
|---|---|---|
| `slice_mons.py` | 몬스터·재료·소품 시트 (가로 N칸 / 2줄 격자) | 아래 두 가지 자동 처리 |
| `slice_hero.py` | 갑옷 전신 (마젠타 봉) | 봉 지우기 + 관통 자리 메우기 + 바닥그림자 제거 |
| `slice_pose.py` | 계열별 자세 (기울어진 봉) | 봉 축에 **수직 방향**으로 메운다 |
| `slice_views.py` | 방향별 그림 (뒷모습·옆모습) | 연결성분 필터 |
| `slice_paw.py` / `crop_paw.py` | 손만 (지금은 안 씀) | |
| `defringe.py` | 글로우 때문에 남은 흰 테두리 제거 | |

### 슬라이서가 자동으로 잡는 함정 (직접 겪은 것)
1. **AI가 그림 밑에 이름표·hex 코드를 써 넣는다.** 말로는 못 막는다.
   → 칸 안에서 **'잉크가 가장 많은 세로 덩어리'만 남긴다** (글자 줄은 얇아서 진다)
2. **균등 분할 시 옆 칸 생물의 끄트머리가 딸려 온다.**
   → **연결성분 중 가장 큰 것만** 남긴다
3. **봉을 지우면 꼬리에 세로 슬롯이 남는다.**
   → 구멍 **양옆 3px 안쪽**에서 색을 떠서 메운다 (가장자리를 퍼오면 창백한 띠가 남는다)
4. **AI가 "no ground shadow" 라고 해도 바닥 그림자를 그린다.** → 발밑 18% 의 거의 흰 화소 제거

## 3-3. 프롬프트 규칙 (실패에서 얻은 것)

- **"실루엣만으로 서로 구분되게"** 를 반드시 넣는다. 없으면 AI가 **넷을 전부 같은 동글이로** 그린다
- 종류마다 **'한눈에 구별되는 특징'** 을 적는다 (`monname.py` 의 `FEATURE` 사전)
- 화풍 문장은 **모든 시트에서 글자 하나 안 바꾼다**. 바꾸면 시트끼리 그림체가 갈린다
- 8마리(2줄×4칸)씩이 왕복 대비 효율이 가장 좋다
- 한글 이름 → 영어 설명은 `monname.py` 가 한다 (148낱말 수동 번역, 미번역 0)

## 3-4. 검사기

- `audit_mon.py` — 몬스터 그림 240장 슬라이스 결함 전수 검사 (가로·세로 덩어리 수 + 채움비)
- `/tmp/t_audit3.js` — 무기 48종 기하 감사 (얼굴 가림 % · 몸통 겹침 % · 손 접촉 · 잘림)

---

# 4. 현재 상태

## 4-1. 완료

| 항목 | 상태 |
|---|---|
| 몬스터 AI 그림 | **240 / 240**, 슬라이스 결함 0 |
| 재료 AI 그림 | **72 / 72** |
| 소품 AI 그림 | **33 / 33** (`PROP_ART` 전부 연결) |
| 무기 그림 | 48종류 전부 고유 (`eq_*` 67장) |
| 무기 손잡이 자동 검출 | 67종 (`gripsolve.py`) |
| 갑옷 6종 '무기 쥔 자세' | 재생성 완료 (마젠타 봉) |
| 뒷모습 | 맨몸 + 갑옷 6종. 위로 걸으면 등을 보임 (계측 30/30 프레임) |
| 스킬·조작 버튼 | AI 그림 + 금속 테두리 + 원형 쿨다운. 공격 20.0mm (NN/g 6~8세 기준 충족) |
| 상태창 | 레퍼런스 실측 2열 배치. 넘침 0 |
| 몬스터 생성 수 | 목표 수만큼만 생성 (413마리 폭주 버그 해결) |

## 4-2. 검증 결과 (최신)

```
회귀 테스트   25 / 25
NaN·예외      0
갤럭시 9기종   UI 벗어남 0 · 시트 넘침 0
  (CPU 조임 : 보급형 6배 · 플래그십 3배 · 구형 7배)
  갤A16 20~28fps · 갤S24 46~55 · 갤A54 27~32 · 갤탭A9+ 19~25 · 갤S8 22~25
무기 48종 감사  얼굴 가림 0% · 잘림 0
```

## 4-3. 남은 작업 (우선순위)

### A. 원리 ④ — 무기 계열별 자세 (가장 중요)
지금 자세는 **주먹이 볼에 붙어** 있다. 그래서 장병기(창·낫·지팡이·빗자루·별지팡이 등 14종)가
**얼굴을 지나간다.** 감사기가 지적한다.

레퍼런스(사장님이 준 상용 스프라이트 시트 13종)는 **팔이 전부 몸 밖으로 뻗어** 있다.

**해야 할 것** : 계열별 자세 그림을 만들어 붙인다 (한손 / 양손 / 장병기 / 활 / 소품).
- 자세 3종 시트는 이미 뽑았으나 **봉에 어두운 외곽선이 그려져** 검은 대각선이 남아 폐기했다
- 다시 뽑을 때 **"봉은 외곽선 없이 납작한 마젠타만"** 을 반드시 넣을 것
- 던파 조사 결론 : 쿼터뷰는 방향 수만큼 물량이 곱해지므로 **계열을 3~4개로 줄이는 게 안전**

### B. 방향별 그리는 순서 표
아래를 볼 땐 무기가 몸 앞, **위를 볼 땐 무기가 몸 뒤**여야 한다.
던파는 횡스크롤이라 이 경우가 없어 참고할 자료가 없다 — **우리가 정의해야 한다.**

### C. 머리 장비(투구) 표시
지금 **꺼져 있다** (`HEAD_OVERLAY=0`). 이유 : `hw_*` 그림이 **얼굴 구멍이 없는 아이콘**이라
겹치면 눈·코·입이 사라진다. 규격에 맞는 머리 레이어를 새로 그려야 켤 수 있다.

### D. UI 화면 4개 (가방·펫·도감·지도) 정비
### E. 펫 시스템 강화
### F. 구글 플레이 출시물
아이콘 · 스크린샷 · 짧은/긴 설명 · 연령등급 설문 · 개인정보처리방침 · 데이터 안전 양식

---

# 5. 검증 방법 (수정 후 반드시 실행)

```bash
cd /root/toriforest
python3 build.py                    # 빌드

node t_regress.js                   # 회귀 25종  → 25/25 여야 함
node t_nan.js                       # NaN·예외    → 0 이어야 함
node t_galaxy.js                    # 갤럭시 9기종 (CPU 조임) → UI벗어남 0
node t_all.js                       # 7기기 종합
node t_perf.js                      # fps

node /tmp/t_hold.js                 # 무기 48종 파지 접촉시트 → /tmp/hold48.png
node /tmp/t_audit3.js               # 무기 기하 감사 (얼굴·몸통·손접촉)
python3 audit_mon.py                # 몬스터 그림 결함 검사
```

⚠ `t_regress.js` 는 드물게 1개가 흔들린다(화면 흔들림 판정). **재실행해서 확인할 것.**

### 눈으로 보는 검증 (이게 제일 중요하다)
숫자가 통과해도 **반드시 렌더해서 본다.** 감사기가 놓친 결함을 여러 번 눈으로 잡았다.
- 무기 48종 : `/tmp/hold48.png`
- 갑옷 6종 착용샷 : `/tmp/t_armhand.js`
- 인게임 : `/tmp/t_armgame.js`

---

# 6. 지금까지 저지른 실수 (같은 실수를 반복하지 말 것)

| 실수 | 진짜 원인 | 교훈 |
|---|---|---|
| 무기가 손 앞에 뜨거나 뒤로 숨음 | 주인공 주먹이 **구멍 없는 '치는 주먹'** | 그림 문제를 숫자로 풀려 했다 |
| 가슴에 크림색 덩어리 | AI 손 그림에 **손바닥·손목**까지 붙어 있었다 | 손가락만 남겨야 한다 |
| 갑옷 끼면 손이 가슴팍에 뜸 | **갑옷 그림 6종이 두 팔을 내린 자세**였다 | 자세가 다르면 좌표가 의미 없다 |
| 칼날이 아래로 향함 | '얇은 쪽=손잡이' 규칙 — **칼끝도 얇다** | 뾰족함으로 판정해야 한다 |
| 투구가 얼굴을 덮음 | `hw_hood` 가 **얼굴 구멍 없는 아이콘** | 겹칠 그림은 같은 배치로 그려져야 한다 |
| 아이템이 칸의 1/4만 차지 | `artCanvas`(ART_FIT 0.72, 발밑 정렬)는 **땅에 선 몬스터용** | 아이콘은 틀을 꽉 채워야 한다 |
| 갑옷 6종이 전부 갈색 | 틴트 캐시 열쇠에 **손 그림 이름이 빠짐** | 캐시 키를 의심할 것 |
| 몬스터 413마리 폭주 | 쿼터를 채우면 보스 플래그가 켜져 무한 리젠 | |
| 무기 그림이 안 바뀜 | `EQ_ART_MAP` 에 **중복 키** — 뒤의 것이 이김 | |
| `element.style.background` 로 금속 테두리가 뭉개짐 | 단축 속성이 **background-clip 을 초기화**한다 | 직후에 다시 지정 |

---

# 7. 코드에서 알아둘 것

## 좌표계
- 주인공 그림은 192px 정사각. **발밑 기준선 `BASE_F = 112/120 = 0.9333`**
- 그리기 : `drawImage(spr, ox - sz*0.5, oy - sz*BASE_F, sz, sz)`
- 손 좌표는 이 기준의 **오프셋** : `fx = ox + sz*hx`, `fy = oy + sz*hy`

## 주요 전역
| 이름 | 뜻 |
|---|---|
| `HERO_GRIP` | 갑옷별 쥐는 점 · 주먹 사각형 (마젠타 봉에서 뽑음) |
| `WEP_AUTO` | 무기별 손잡이 좌표·자루 각도 (`gripsolve.py` 결과) |
| `WEP_CLASS` / `WEP_RULE` | 무기 → 계열 / 계열 → 기울기·크기 |
| `POSE_ANG` | 주인공 자세의 자루 방향 (지금 -1.5708 = 수직) |
| `PROP_ART` | 소품 종류 → 그림 파일 |
| `HEAD_OVERLAY` | 투구 겹치기 on/off (지금 0) |
| `_DIRSPR` | 방향별 그림 캐시 (뒷모습) |

## 그림 로딩
`artLoad()` 는 **첫 화면에 필요한 것만 기다린다.** 몬스터 240 · 재료 72 는 뒤에서 받는다.
늦게 도착하면 해당 캐시만 버려서 다음에 그릴 때 그림 판이 쓰이게 한다
(`artIsCritical` / `artLateArrive`). 이걸 안 하면 보급형 폰 부팅이 6~7초다.

---

# 8. 프로젝트 문서 (claude.ai 프로젝트에 저장돼 있음)

- `도토리숲-현재상태.md`
- `도토리숲-무기파지-실측.md` — 레퍼런스 실측값 (검 길이/키 0.466 · 자루 0.263 · 각도 +30.1° 등)
- `도토리숲-그림파이프라인-버튼규격.md` — 터치 크기 기준(NN/g 6~8세 20mm 등)
- `던전앤파이터-방식-조사.md` — DNF 1차 자료 전수 조사 (출처 전부 포함)

---

# 9. 사장님 PC 환경

- Windows, `desktop-f3pcj6k`
- 연결 폴더 : `C:\Users\sun\Downloads`
- `clipsave.ps1` 이 Downloads 에 있다 (클립보드 → PNG)
- **게임 소스와 그림은 사장님 PC에 없다.** 전부 작업 컨테이너 `/root/toriforest` 안에 있다
  (`C:\Users\sun\toriart` 는 `recv.py` 하나에 `sheets` 폴더는 비어 있음)

---

# 10. ⚠ 인수인계 꾸러미 (이걸 새 계정에 올려야 이어서 할 수 있다)

**작업 컨테이너는 세션이 끝나면 사라진다.** `/root/toriforest` 는 새 계정에서 볼 수 없다.
그래서 아래 파일을 사장님 PC로 내려보냈다.

```
도토리숲_인수인계.zip   (C:\Users\sun\Downloads)
├─ HANDOVER.md                이 문서
├─ toriforest/
│   ├─ game.html              ★ 단일 원본 (1.26MB)
│   ├─ build.py               빌드 (art 를 base64 로 주입 → dotorisup.html)
│   ├─ art/                   AI 그림 전부 (522개 : 몬스터240·재료72·소품33·무기67·주인공·UI)
│   │   ├─ wep_grip.json      무기 67종 손잡이 좌표 (gripsolve.py 결과)
│   │   └─ hero_grip.json     갑옷 7종 쥐는 점·주먹 사각형 (마젠타 봉 결과)
│   ├─ icons/                 앱 아이콘
│   ├─ *.py                   슬라이서·검출기·프롬프트 생성기 전부
│   ├─ *.js                   엔진 조각·테스트
│   └─ tests/                 t_*.js (회귀·갤럭시·감사)
└─ apk/                       안드로이드 빌드 (AndroidManifest·build.sh·서명키 tori.jks)
```

## 새 계정에서 이어받는 순서

1. 이 zip 을 새 대화에 올린다 (또는 연결 폴더에 두고 스테이징한다)
2. `unzip 도토리숲_인수인계.zip -d /root/` → `/root/toriforest`, `/root/apk` 로 푼다
3. `HANDOVER.md` 의 **0. 절대규칙**을 먼저 읽는다
4. `cd /root/toriforest && python3 build.py` 로 빌드가 되는지부터 확인한다
5. `node tests/t_regress.js` → **25/25** 나오는지 확인한 뒤에 손댄다
   (테스트가 `/tmp/t_*.js` 를 찾으면 `cp tests/t_*.js /tmp/` 해 준다)
6. `4-3 남은 작업` 의 **A(무기 계열별 자세)** 부터 시작한다

**포함하지 않은 것** : git 이력(236MB), 스크린샷 폴더, 빌드 산출물(dotorisup.html·apk).
전부 `build.py` / `apk/build.sh` 로 다시 만들 수 있다. **원본과 그림은 하나도 안 빠졌다.**

# 11. 2026-08-31 (세션 3) 작업 요약

## 11-1. 절대규칙 8번 진행 — AI 조합 그림 12종

맨몸+경갑(light) × 투구 6종 대표 조합을 제미나이로 생성 → `slice_combo.py`로 192×192
프레임에 슬라이스 → `heroGeared()`에 "0단계"로 조합 그림 우선 사용 로직 추가
(`art/hero_<armorClass|bare>_<headType>.webp` 있으면 통째로, 없으면 기존 오버레이로 폴백).
검증: `tests/t_combo_verify_all12.js`. 남은 조합: 384/396(다른 갑옷 4계열×투구6종 + 원소
11색 변형) — 다음 세션.

## 11-2. 실측으로 발견·수정한 렌더링 버그 2건 (콤보 작업과 무관하게 이미 있던 버그)

- **버그 A**: `artCanvas()`가 이미 완성된 프레임인 `heroW_*`·콤보 그림을 또 0.72배
  축소·재배치해서, 갑옷 착용 시(대표조합 외) 투구가 머리 위에 붕 떠 보였다. 이미 완성된
  그림 전용 `artFill()`(1:1 그대로 그리기)을 추가해 교체. 검증: `tests/t_cap_isolate.js`.
- **버그 B**: 맨몸 크기보정 코드의 목표 범위 상수가 버그 A 때문에 "이중 축소된" 모습 기준으로
  잘못 실측돼 있었다. `art/heroW_light.webp` 알파 bbox를 직접 재실측(x34~156,y3~178)해서
  교체. 부수효과: 맨몸-갑옷 캐릭터 크기 불일치(예전 지적사항)도 같이 해결.

두 버그 다 `python3 build.py` 재빌드 후 전체 회귀 테스트 재통과 확인.

## 11-3. 무기 강화(+1~+15) 이펙트 + 원소별 셰이프 랭귀지

- 기존 `drawEnhanceAura()`(장비 아이콘·손에 든 무기 스프라이트)는 이미 강화 단계별
  연출(링→깃털→번개→반짝임)이 있었음을 확인.
- `fxSlashArc()`(근접 베기 궤적, 스킬 다수가 공유)에 무기 +강화 수치를 반영 — 조각 수·
  섬광 크기·잔불티가 강화도에 비례. +12부터 큼직한 원소 플레어 추가.
- `skillCastFX()`(전 무기 계열 공통 스킬 캐스팅 지점 — 지팡이·망치·활 등)에도 같은 원소·
  강화 연출 추가.
- **원소별 모양 차별화**: 11원소를 5계열로 묶어 기존 스프라이트 재사용
  (불꽃=fire/pumpkin→`SPR.fx.flame`, 서리=ice/wind/water→`SPR.fx.ice`,
  자연=leaf/stone→`SPR.fx.leaf`, 광채=light/star/acorn→`SPR.fx.spark2`,
  그림자=shade→`SPR.fx.smokeP`). +0 구간부터 항상 나가는 파편도 색뿐 아니라 모양까지
  원소별로 다르게(개수는 그대로라 화면 혼잡도 증가 없음).
- `hitStop()`에 강화 비례 보너스(최대 +10ms) 추가.
- 리서치 근거: `research_skill_vfx_2026-08-31.md`(claude.ai 프로젝트에 저장, vfxapprentice·
  gdquest 등 VFX 업계 원칙 벤치마킹).
- 검증: `tests/t_atk_enhance_fx.js`, `tests/t_atk_elem_family.js`, `tests/t_atk_zero_elem.js`,
  `tests/t_skillfx_archetypes.js`(지팡이·망치·활). 전부 스크린샷 대조, 에러 0.

## 11-4. 남은 작업

- 절대규칙 8번 콤보 그림 나머지 384개(다른 갑옷 계열 + 원소 11색).
- 나머지 무기 8계열(great/dagger/katana/axe/spear/scythe/claw/boomer 등)은 `skillCastFX`
  공통 경로를 타므로 구조상 같은 효과를 받지만 개별 스크린샷 검증은 안 함.
- `HEAD_LAYER_BARE`/`hwbare_*` 죽은 코드 정리(미착수, 낮은 우선순위).

# 12. 2026-08-31 (세션 4) 작업 요약

**진행 승인**: "일단 진행해. 오늘 안끝나도 된다" — 완료 압박 없이 이어서 진행.

## 12-1. 절대규칙 8번 계속 — 나머지 갑옷 5계열 × 투구 6종 = 30개 조합 그림 완성

세션 3에서 만든 bare·light 12개에 이어, **plate·mage·scale·royal·dark 5계열 × 투구
6종(cap/helm/hornh/hoodh/mask/crownh) = 30개**를 제미나이(Ultra)로 전부 생성 →
`slice_combo.py`로 192×192 슬라이스(고정 박스 34,3~156,178) → `art/hero_<계열>_<투구>.webp`.

**결과: 원소 기본색(acorn) 기준 갑옷 7계열(bare 포함) × 투구 6종 = 42개 조합 그림 전부
완성.** (원소 11색 변형은 별도 범위 — 12-4 참고, 이번 세션 미착수)

## 12-2. ⚠ 실측으로 발견한 심각한 버그 — 제미나이 다운로드 버튼이 "이전 그림"을 내려받음

절대규칙 6번(추측 금지)에 따라 매 조합을 빌드 후 실제 게임 화면에서 스크린샷으로
재검증하던 중, **royal 계열 2개(mask·crownh)와 dark 계열 전부(6개), 총 8개**에서
투구가 파일명과 다른 것으로 나타남을 발견 (예: `royal_mask.webp`를 게임에서 씌우면
마스크가 아니라 후드가 보임).

**처음 오진단**: 캔버스 캡처 타이밍/비동기 레이스 문제로 의심 → `t_combo_verify_5class30.js`를
`evaluate()` 분리 + `waitForTimeout(80)` 지연 추가로 재작성 → **효과 없음** (증상 그대로).
이걸로 게임 로직 문제가 아님이 증명됨.

**정확한 원인 규명**: `sha256sum`으로 `art/*.webp` 파일끼리 직접 비교 →
`hero_royal_mask.webp`가 `hero_royal_hoodh.webp`와 완전히 동일한 파일(바이트 단위
중복)임을 확인. 이어서 30개 전체를 5×6 그리드로 합성해 육안 감사 → **royal 마지막
2개, dark 전체 6개가 "한 칸씩 밀려서" 바로 이전 조합의 그림을 담고 있는 패턴**을
발견 (dark_cap이 실제로는 royal_crownh 내용, dark_helm이 실제로는 진짜 dark_cap
내용... 순서대로 한 단계씩 밀림).

**근본 원인**: 제미나이 웹 UI에서 새 그림이 화면에 뜬 직후 다운로드 버튼을 누르면,
화면에는 새 그림이 정확히 보이는데도(직전 스크린샷으로 확인했음에도) **다운로드는
간헐적으로 대화 이력의 "이전" 그림을 내려받는** 제미나이 자체 UI/백엔드 타이밍
버그. 테스트 하네스나 game.html 코드의 문제가 아니었음.

**재발 방지 — 새 검증 절차 채택**: 다운로드 직후 파일을 스테이징 → PNG 변환 →
**Read 도구로 실제 변환된 파일을 직접 눈으로 확인** → 통과해야만 `srcart/`·`art/`에
반영. 기존에는 "화면에 보이는 것"을 다운로드 파일의 대리 증거로 신뢰했는데, 이번에
그 신뢰가 틀렸음이 증명됐으므로 **화면 확인과 다운로드 파일 확인을 분리**하는 것으로
프로토콜을 강화함. 8개 전부 이 절차로 재생성·재확인 완료, 재빌드 후 `sha256sum`으로
42개 조합 그림 전체 해시 중복 없음 재확인.

## 12-3. 재검증

- `tests/t_combo_verify_5class30.js` (신규): 30개 조합을 실제 게임(`__TORI` API)에서
  장착 → 캔버스 캡처 → 30/30 성공, 에러 0, 크기 192×192 전부 정상.
- `tests/t_debug_combo_lag.js` (신규, 디버그용): 장비 상태(`eqOn`, `EQUIP[].tn`)가 매
  단계 정확히 요청한 투구와 일치함을 확인 — 게임 로직은 처음부터 문제 없었음을 증명.
- `python3 build.py` 재빌드: "그림 569장 주입(11.1MB)", `dotorisup.html` 12,597,632 bytes.
- `bash verify.sh quick` 전체 재실행:
  - 정적 분석: 구문 정상(19030줄), 중복선언 없음, 도달불가 코드 2곳(11764·14788줄)은
    둘 다 **의도적으로 비활성화한 죽은 코드**(`spreadCamps_DISABLED` 주석 처리,
    `doUlt` 필살기 사용자 요청으로 삭제 — 주석에 명시됨) — 이번 세션 변경과 무관, 회귀 아님.
  - 회귀 25종: **25/25 통과**.
  - NaN·예외: 감시 449회, **NaN/Infinity 없음, 예외 없음**.
  - 글꼴 오프라인: **정상**(외부요청 0, 서브셋 3종 적용).
  - `tests/t_combo_fallback_regression.js`: 케이스 A/B/C 전부 192×192 정상, 에러 없음.
- 30개 스크린샷 육안 재확인(특히 이전에 깨졌던 royal_mask·royal_crownh·dark 전체 6개)
  — 전부 파일명과 실제 투구 종류 일치 확인.
- `t_audit_armor.js`(갑옷×무기 겹침 감사)의 ❌ 표시들은 `bare`(맨몸)에서도 동일하게
  나타나는 무기 손잡이 겹침 수치라 이번 콤보 그림 작업과 무관한 기존 특성(무기 자세
  쪽 이슈, 갑옷/투구 조합 그림과 무관) — 참고로만 남김, 이번 세션 회귀 아님.

## 12-4. 남은 작업 (다음 세션)

- **원소 11색 변형**: 절대규칙 8번의 전체 범위(384/396 중 나머지)에 포함되는 원소별
  색상 변형은 이번 세션에서 다루지 않음 — 규모가 크고(7계열×6투구×11원소) 사장님
  확인 없이 추측으로 범위를 정하지 말 것. 착수 전 반드시 확인.
- `HEAD_LAYER_BARE`/`hwbare_*` 죽은 코드 정리(미착수, 낮은 우선순위, 세션 3부터 이월).
- **제미나이 다운로드 검증 절차(12-2)를 앞으로 모든 AI 그림 생성 작업에 표준 적용할 것**
  — 화면 확인만으로는 부족하고, 반드시 다운로드된 실제 파일을 열어 확인해야 함.

# 13. 2026-08-31 (세션 5) 작업 요약

**진행 승인**: "그냥 니가 이어서 진행해라" — 원소 11색 변형(12-4 남은 작업) 중
**fire(불꽃)부터** 먼저 전체 완성하라는 지시로 착수. "작업 멈추지말고 이 대화창에서
완성본 만들어줘"라는 재확인 지시에 따라 중단 없이 이어서 진행.

## 13-1. fire(불꽃) 원소 콤보 그림 36/36 완성

갑옷 6계열(plate/mage/scale/royal/dark/light) × 투구 6종(cap/helm/hornh/hoodh/mask/
crownh) = **36개 전부** 제미나이(Ultra)로 생성 → 12-2에서 확립한 강화 검증 절차
(다운로드 즉시 스테이징 → Read 도구로 실제 파일 픽셀 직접 확인 → 통과 후에만 반영)
그대로 적용 → `slice_combo.py`로 192×192 슬라이스 → `art/hero_<계열>_<투구>_fire.webp`.
원본은 `srcart/hero_<계열>_<투구>_fire_src.png`로 보관.

- **light 계열 재확인**: 12-1에서 light는 acorn(기본색) 6개만 있고 fire 등 원소 변형은
  없었음을 `ls art/hero_light_*.webp`로 재확인 → 36개 범위(6계열×6투구)에 light가
  포함됨을 확정하고 이번 세션에서 완성.
- 검증: `ls art/hero_*_fire.webp | wc -l` → 36. 계열별 6/6 확인.
  `md5sum art/hero_*_fire.webp` 해시 전부 유일(중복 0건) — 12-2에서 겪은 "이전 그림
  다운로드" 버그 재발 없음.
- 발견·수정된 색상 오류 1건: royal+fire+hornh 1차 생성분의 투구 금속이 회색/은색으로
  나와(요청한 ember-copper/bronze 미적용) 육안 확대 검사로 발견 → 같은 제미나이
  대화창에서 "투구 금속만 갑옷과 어울리는 ember-copper/bronze로, 뿔은 진한
  maroon/burgundy 유지" 식으로 보정 재요청 → 재생성 확인 완료.
- **알려진 문제(작업 방식, 이번 세션 2회 발생)**: Chrome 탭에서 `computer` 도구의
  `zoom` 액션 사용 후 뷰포트가 갑자기 극소 크기(319×220 등)로 줄어드는 버그 발견.
  `resize_window`로는 복구 안 됨 — **탭을 새로 만들어 같은 제미나이 대화 URL로
  재접속하는 것만 해결됨**. 앞으로 `zoom` 액션 사용을 피하고 전체 화면 스크린샷 +
  육안 확인으로 대체할 것.

## 13-2. `heroGeared()` 콤보 그림 매칭 로직을 원소별로 확장

기존 로직은 갑옷·투구가 **둘 다 acorn(기본색, en===0)일 때만** 사전제작 콤보 그림을
사용했음 (`headEq0 && (headEq0.en|0)===0 && (!ea0 || (ea0.en|0)===0)`). 이번에 갑옷과
투구가 **같은 원소로 일치**하면(맨몸일 땐 기존대로 acorn 투구만) 콤보 그림을 쓰도록
확장:

```js
var headEn0 = headEq0? (headEq0.en|0) : 0;
var armEn0 = ea0? (ea0.en|0) : 0;
if(headEq0 && (cls0? (armEn0===headEn0) : (headEn0===0))){
  var hT2=SLOT_TYPES[headEq0.slot];
  var headType0=(hT2 && hT2[headEq0.tn])? hT2[headEq0.tn].id : null;
  if(headType0){
    var elemId0=(ELEM[headEn0]||ELEM[0]).id;
    comboKey='hero_'+(cls0||'bare')+'_'+headType0+(headEn0===0? '' : ('_'+elemId0));
    comboImg=artOf(comboKey);
  }
}
```

파일명 규칙: acorn은 접미사 없음(`hero_<계열>_<투구>.webp`, 기존 42개 그대로 호환),
그 외 원소는 `hero_<계열>_<투구>_<원소id>.webp`. 아직 안 만든 조합은 `artOf()`가
`null`을 반환해 **기존 색조 회전(hue-rotate) 오버레이 방식으로 자동 폴백** — 그림이
없다고 깨지거나 안 보이는 일 없음.

## 13-3. 재빌드 및 회귀 검증

- `python3 build.py`: "그림 605장 주입 (11.6 MB)", `dotorisup.html` 13,094,779 bytes.
- `bash verify.sh` 계열 (수동, `NODE_PATH=/opt/node-tools/node_modules` 직접 지정 —
  `verify.sh` 자체의 `NODE_PATH=$(npm root -g)`는 이 환경에서 안 맞음):
  - `t_static.js`: 통과. 도달불가 코드 2곳(11764·14788줄)은 12-3에서 이미 확인한
    의도적 죽은 코드(비활성화된 `spreadCamps_DISABLED`, `doUlt`) — 이번 세션과 무관.
  - `t_regress.js`: **25/25 통과**.
  - `t_nan.js`: 감시 434회, **NaN/Infinity 없음, 예외 없음**.
- **신규 종단 검증 `tests/t_fire_combo_verify.js`**: 6계열×6투구 36개 조합을 실제
  게임(`__TORI` API)에서 장착 → `T.artOf('hero_<계열>_<투구>_fire')`로 콤보 그림이
  실제로 해석(resolve)되는지 직접 확인 → 캔버스 캡처. **결과: 36/36 캡처, 36/36
  콤보 해석 성공(comboResolved), 에러 0, 종료 코드 0.**
  - 캡처된 스크린샷 중 `plate_h_cap_fire`, `royal_h_crownh_fire`, `light_h_hoodh_fire`
    3장을 Read 도구로 직접 눈으로 확인 — 갑옷·투구가 자연스럽게 하나로 붙은 불꽃
    색상의 전신 그림이 정확히 렌더링됨을 확인 (모자류가 안 보이거나 투구가 머리와
    떠 보이는 절대규칙 8번의 원래 문제 없음).
  - (참고: 최초 작성한 검증 스크립트는 `T.EQUIP` 키를 `k.startsWith(...)`로 검색하는
    잘못된 로직 때문에 0/36으로 실패했었음 — 실제로는 `h_cap_fire` 같은 이름이 바로
    `T.EQUIP`의 키였음을 `Object.keys(T.EQUIP).filter(...)`로 재확인 후 직접 대입하는
    방식으로 수정하여 재실행, 통과.)

## 13-4. 결론 — fire 원소 완성 (1차 완성본)

**갑옷 6계열 × 투구 6종 × fire 원소 = 36/36 콤보 그림 완성, `heroGeared()` 배선 완료,
실제 게임 렌더링까지 종단 검증 통과.** 기존 acorn 42개 콤보와 기존 게임 동작(회귀
25/25)에 영향 없음 확인.

## 13-5. 남은 작업 (다음 세션)

- **원소 9종 남음**: ice, leaf, wind, stone, water, light(원소 — light **갑옷 계열**과
  이름이 겹치니 혼동 주의), shade, star, pumpkin. 각 원소마다 이번 세션과 동일한
  절차(6계열×6투구=36장 생성 → 검증 → 반영 → 재빌드 → 종단 테스트)를 반복 —
  9원소 × 36장 = 324장 남음 (전체 396장 중 72장 완료: acorn 42 + fire 36... 단, acorn은
  bare 포함 42장이고 fire는 bare 제외 36장이므로 절대규칙 8번 총 목표는
  7계열×6투구×11원소 기준 재확인 필요 — bare에 원소별 변형을 포함할지는 사장님
  확인 후 착수).
- 페이스 참고(사장님 질문에 답한 실측치): 이미지 1장당 약 70~90초 실측, 순수 생성
  시간만 9시간 안팎 예상 — 여러 세션에 걸쳐 진행될 대규모 작업임을 사장님도 인지.
- `zoom` 컴퓨터 액션 사용 자제(13-1 참고, 탭 뷰포트 축소 버그).
- `HEAD_LAYER_BARE`/`hwbare_*` 죽은 코드 정리(미착수, 낮은 우선순위, 세션 3부터 이월).

# 14. 2026-08-31 (세션 6) 작업 요약

## 14-1. pumpkin(호박불) · light(햇살) · ice(서리) 3개 원소 콤보 그림 108/108 완성

사용자 지시("pumpkin, light, ice 만 먼저 해라. 그러고나서 1차 완성본 줘")에 따라
세 원소 각각 갑옷 6계열 × 투구 6종 = 36장씩, 총 108장을 fire 원소와 동일한 파이프라인
(Gemini 생성 → `javascript_tool` blob 다운로드 → `device_stage_files` → `Read` 툴로
실측 검증 → `slice_combo.py` 슬라이스 → `art/`·`srcart/` 반영)으로 완성했다.

- `art/hero_<class>_<head>_pumpkin.webp` 36/36
- `art/hero_<class>_<head>_light.webp` 36/36 (⚠ "light" 갑옷 계열과 이름이 겹치는
  "light" **원소** — 혼동 주의, 13-5절에서 이미 지적된 사항)
- `art/hero_<class>_<head>_ice.webp` 36/36

**세션 시작 시점 확인 결과, 요약이 말한 "18/36 완료(plate/mage/scale)"가 실제로는
부정확했다** — 실측(`ls`+per-class 카운트)해보니 plate 5/6(mask 누락), scale 4/6
(mask·crownh 누락)이었다. 원인: 이미지는 생성·다운로드·`Read` 검증까지 끝났는데 마지막
`cp`+`slice_combo.py` 반영 단계를 건너뛴 경우(plate+mask — PC Downloads에 파일은
남아 있어 재슬라이스만으로 복구)와, 이미지 자체가 아예 생성되지 않은 경우(scale+crownh —
새로 생성)가 섞여 있었다. **교훈: "생성했다"는 요약 문구를 믿지 말고 항상
`ls art/hero_*_<elem>.webp | wc -l` 실측으로 재확인할 것** (절대규칙 5번과 같은 원칙).

royal+cap(ice)은 세션 재개 시점에 "정정 프롬프트를 보냈고 스크린샷상 성공한 것처럼
보이나 다운로드·검증·반영 전" 상태였다 — 이어서 다운로드→실측 검증(케이프가 뚜렷하게
보이고 매끈한 판금 갑옷인지 확인)→반영까지 마무리했다.

royal+crownh(ice)에서 재발한 드리프트: 크라운 교체 요청 시 갑옷 색이 원소색(icy blue)
대신 원본 "GOLDEN royal armor" 문구에 이끌려 금색으로 되돌아갔다 — "That was wrong -
the armor came out gold colored again... NOT gold/yellow... same colors as the
cap/helm/hornh/hoodh/mask ice combos already made" 식으로 재수정해 해결(1회 정정으로
성공). **새로운 교훈**: "same X from the previous image" 패턴이 실루엣 드리프트는 잘
막아도, 프롬프트에 원래 계열명(예: "GOLDEN royal knight armor")을 색상 설명 없이
그대로 남겨두면 원소색 대신 그 단어의 원래 색으로 색이 되돌아갈 수 있다 — 색 재지정이
필요한 매 프롬프트마다 "NOT <원래색>"을 명시하는 게 안전하다.

## 14-2. ⚠ 검(sword) 기본 공격이 원소에 따라 화면을 가로지르는 사거리 버그 발견·수정

세션 도중 사용자가 실제 플레이 확인 후 지적: "검 평타 공격은 던전앤파이터의
웨펀마스터 같은 느낌으로 해야한다. 그리고 칼 공격은 너무 멀리까지 닿으면 이상한거
알지?" (+ "던파의 버서커 같은 느낌도 좋다").

**원인 조사 결과(코드 직접 대조, 추측 아님)**: `buildAbils()`(game.html ~2569행)가
검 아키타입(`ARCHE[0]`, id `'blade'`, `ARCHE[a].kinds`엔 `['melee','melee','spin']`로
명시돼 있었으나 이 필드는 실제로 안 쓰이고 있었다)의 실제 `kind`를
`KP=['melee','spin','orbit','laser','chain']` 배열에서 `e%5`(원소 인덱스)로 순환
배정한다 — 즉 11원소 중 `laser`(사거리 760)나 `chain`(사거리 300)이 걸리는 원소는
겉보기엔 평범한 "검"인데 실제 타격 판정과 시각 이펙트(`weaponSwingFX`가 `A.kind`와
무관하게 항상 검 슬래시 모양을 그림)가 화면 크기에 육박하는 범위까지 뻗어나갔다.
`castSkillCore`의 `rng=A.range[t]*(A.skill?0.5:1)*SC`에서 기본 공격(`A.skill`
false)은 스킬과 달리 사거리 축소(0.5배)조차 안 걸려 더 심했다.

**수정**(game.html, `buildAbils()` 내부 1줄 + 주석): 검 아키타입(`a===0`)에서만
`kRng=Math.min(kRng,210)`으로 사거리 상한을 걸었다. kind별 연출·판정 방식(레이저 광선
모양, 사슬 번개 등)은 그대로 살리되 숫자만 근접 무기다운 범위로 압축 — tier2(+강화
최대) 기준 최대 사거리가 laser 1018→281, chain 402→281로 줄었다(근접 기본값
203.7과 비교해 +38% 수준, 이전엔 최대 +400%). `kind='melee'`(기존 152)·`'orbit'`
(기존 126)는 원래도 상한 이하라 변화 없음. 지팡이(`cast`)·망치(`heavy`) 아키타입은
건드리지 않음(원래도 원거리·범위 성격이 맞는 무기).

**검증**: 신규 `/tmp/t_blade_range_check.js`(검 아키타입 11개 abil의 `A.range` 실측 —
수정 후 tier2 최대 281.4 확인), 신규 `/tmp/t_blade_visual.js`(실제로 sword 무기 +
laser-kind였던 `leaf_blade` abil을 장착하고 원거리 적을 공격 → 스크린샷으로 확인,
피격 데미지 정상 적용, 에러 0), `t_regress.js` 25/25, `t_nan.js` 451회 감시 이상무.
`tests/t_pli_combo_verify.js`(아래 14-3절)도 이 변경 이후 재실행해 통과 확인.

**하지 않은 것**: "웨펀마스터/버서커 같은 타격감"이라는 더 큰 주문(콤보 타이밍,
히트스톱 강화, 모션 차별화 등)은 이번 세션에서 손대지 않았다 — 사거리 버그는 명확한
결함이라 바로 고쳤지만, 타격감 재설계는 실제 플레이 스크린샷·반복 튜닝이 필요한
별도 작업이라 108장 원소 그림 완성(사용자가 명시적으로 우선순위를 준 작업)을 먼저
끝내는 쪽을 택했다. **다음 세션 최우선 후보**로 남겨둠 — `DNF-Kirby-BurningStone-
전투벤치마킹.md`(claude.ai 프로젝트)에 이미 던파 웨펀마스터/붉은보석 벤치마킹 자료가
있으니 그걸 기반으로 시작하면 된다.

## 14-3. 재빌드·회귀·종단 검증

```
python3 build.py                  # 그림 713장 주입, dotorisup.html 14.6MB
node t_regress.js                 # 25/25
node t_nan.js                     # 451회 감시, NaN·예외 없음
node --check <script 추출>         # acorn 모듈이 net 제한으로 설치 불가해 t_static.js
                                   # 대신 문법 검사만 별도 수행 — 문법 오류 없음
node tests/t_pli_combo_verify.js  # 신규 작성 — pumpkin+light+ice 108/108 콤보 전부
                                   # T.artOf() 로 정상 해석 확인, 에러 0
```

## 14-4. 결론 — pumpkin·light·ice 3원소 완성 (1차 완성본)

**갑옷 6계열 × 투구 6종 × 3원소(pumpkin/light/ice) = 108/108 콤보 그림 완성**,
검 사거리 버그 수정까지 포함해 재빌드·회귀·종단 검증 전부 통과. 기존 acorn 42개 +
fire 36개(이전 세션)에 영향 없음 확인. **누적: acorn 42 + fire 36 + pumpkin 36 +
light 36 + ice 36 = 186장.**

## 14-5. 남은 작업 (다음 세션)

- **원소 6종 남음**: leaf, wind, stone, water, shade, star. 원소마다 6계열×6투구=36장
  (총 216장 남음) — 13-5절과 동일한 절차 반복.
- **[신규, 우선순위 사용자 판단]** 14-2절의 "웨펀마스터/버서커 같은 타격감" 재설계 —
  `DNF-Kirby-BurningStone-전투벤치마킹.md` 참고해서 시작.
- `zoom` 컴퓨터 액션 사용 자제(13-1 참고).
- `HEAD_LAYER_BARE`/`hwbare_*` 죽은 코드 정리(미착수, 낮은 우선순위).
- (권장) `acorn` npm 패키지를 설치 가능한 세션에서 한 번 받아 캐시해 두면 `t_static.js`
  문법 검사를 원래 방식대로 돌릴 수 있다 — 이번 세션은 net 제한으로 우회 검증만 했음.
