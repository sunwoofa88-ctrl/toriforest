# 「도토리숲 대모험」 인수인계서

작성 2026-08-30 · 작성자 Claude (Opus 5)
다음 담당자에게 그대로 넘긴다. **먼저 「0. 절대규칙」을 읽고 시작할 것.**

---

# 0. 절대규칙 (사장님 지시 · 최우선 · 예외 없음)

1. **모든 작업은 초고퀄리티·초고도화로 정밀하게 진행**
2. **오류 절대 발생 금지**
3. **수정 → 확인 → 검증 → 재검증 단계를 반드시 거칠 것**
4. **UI·해상도 등 모든 것이 최적화·시인성·직관성 우수할 것**
5. **벤치마킹 가능한 인터넷 자료를 검색·수집·분석·확인·적용·검증·재검증**
6. **추측에 의한 작업 절대 금지 — 사실·데이터 근거가 있는 작업만**
7. **매 작업 시 타 게임 상위 20개를 조사, 비교 분석, 정확·정밀하게 벤치마킹**

## 0-1. 추가 상시 제약

- **갤럭시 탭 A9+ 에서 쾌적하게 작동** (끊김·멈춤·강제종료 없을 것)
- **갤럭시 스마트폰들에서도 잘 돌아가야 한다** (보급형 A 시리즈 포함)
- **게임 내 모든 이미지는 AI 생성이어야 한다.** AI 생성이 아닌 게 게임 안에 하나도 없어야 한다
- 게임 용량 300MB 이상 되어도 됨 (현재 APK 11MB)
- **저작권** : 오리지널 캐릭터만. 레퍼런스는 디자인 언어 학습용으로만 본다
- **6~9살 아이들이 극도로 좋아할 게임**을 만들 것. 관련 자료를 모조리 검색·조사할 것
- 무기 잡은 모습·착용한 모습·휘두르는 모습·공격 모습 전부 **타 2D게임 상위 20개와 동급 퀄리티**
- **모든 무기 쥐었을 때 이상하면 절대 안 됨**
- 최종 목표 : **구글 플레이 출시** (6~10세, 3,300원)

## 0-2. 일하는 방식 (사장님이 반복해서 요구한 것)

- **"하나 고치면 하나 터지는" 것을 멈춰라.** 증상이 아니라 **원리**를 고쳐라
- 눈대중 금지. **실제 파일을 픽셀로 재고, 렌더해서 눈으로 확인**한 뒤에 말할 것
- 모르면 모른다고 하고, 틀렸으면 틀렸다고 할 것

---

# 1. 프로젝트 개요

| 항목 | 값 |
|---|---|
| 게임 | 「도토리숲 대모험」 쿼터뷰 육성 RPG |
| 대상 | 사장님 아들(7세), 구글 플레이 6~10세 |
| 주인공 | 다람쥐 (주황 털 #E8873C, 크림 얼굴·배 #F6E3C0, 초록 목수건, 외곽선 #3A2418) |
| 원본 | `/root/toriforest/game.html` (약 1.26MB, 단일 HTML) |
| 빌드 | `python3 build.py` → `dotorisup.html` (art/*.webp + 글꼴을 base64로 주입, 12.5MB) |
| APK | `cd /root/apk && bash build.sh` → `도토리숲 대모험.apk` (11.1MB) |
| 형상관리 | `/root/toriforest` 가 git 저장소. 커밋 메시지에 **원인과 근거**를 적을 것 |

### 콘텐츠 규모
- 지역 20 · 장 60
- 몬스터 **240종** (지역당 12 : 일반 8 · 정예 3 · 보스 1)
- 무기 48종류 / 장비 아이템 220종 · 방어구 110종
- 재료 72종 (12계열 × 6형태) · 소품 33종
- 능력 30종 × 3단계

---

# 2. ★ 핵심 아키텍처 — 무기·방어구 착용 원리

> 이게 이 프로젝트에서 가장 오래 헤맨 부분이다. **원리를 이해하지 못하면 또 헤맨다.**

## 2-1. 어떤 무기·방어구를 껴도 안 깨지려면 (상용 엔진 문서 조사 결론)

| | 원리 | 상태 |
|---|---|---|
| ① | **캐릭터 그림마다 '쥐는 자리'가 숫자로 선언돼 있어야 한다** (좌표·자루 각도·굵기) | ✅ |
| ② | **무기 그림마다 '손잡이 자리'가 같은 규격으로 선언돼 있어야 한다** | ✅ |
| ③ | **그리는 순서가 규칙이어야 한다** (몸 → 무기 → 앞손) | ✅ |
| ④ | **자세는 무기 '계열'이 고른다** (계열은 유한, 계열마다 ①을 따로 가짐) | ⏳ 미완 |

근거 : Spine(뼈·슬롯·스킨) · Cocos Creator(Socket) · Unity(BoneFollower / Sprite Library) ·
Godot(컷아웃) · Live2D(재료 분리) · Adobe(레이어 분리) · Mana Seed(동일 배치 규칙).
**던전앤파이터도 같은 구조다** — 자세한 것은 프로젝트 문서 `던전앤파이터-방식-조사.md` 참고.

## 2-2. 마젠타 봉(magenta rod) 기법 — 이 프로젝트의 핵심 도구

AI에게 캐릭터를 그리게 할 때 **손에 납작한 마젠타(#FF00FF) 봉을 쥐게 한다.**
그 봉을 지우면 **쥐는 점 · 자루 각도 · 자루 굵기 · 주먹 사각형**이 그림에서 그대로 나온다.
**추측이 개입할 여지가 구조적으로 없다.**

- 슬라이서 : `slice_hero.py` (갑옷 전신) · `slice_pose.py` (계열별 자세) · `slice_paw.py` (손만)
- 결과 : `art/hero_grip.json` (갑옷 6종 + 맨몸의 쥐는 점·주먹 사각형)

### ⚠ 반드시 지킬 프롬프트 규칙
> **"봉은 외곽선 없이 납작한 마젠타만 (flat magenta, NO outline)"**

AI가 봉에 **어두운 외곽선**을 그리면 색으로는 절대 못 지운다. 검은 대각선이 남는다.
(자세 시트 3종이 이것 때문에 아직 게임에 못 들어갔다.)

## 2-3. 무기 손잡이 자동 검출 — `gripsolve.py`

**예전엔 무기 48종의 좌표를 손으로 박아 넣었다(48개의 예외). 그래서 하나 고치면 하나가 틀어졌다.**
지금은 그림에서 직접 잰다:

1. 실루엣의 **주축**을 주성분분석(PCA)으로 구한다 → 자루 방향
2. 주축을 따라 **두께 단면도(20칸)** 를 낸다
3. **손잡이 판정** : "얇은 쪽"이 아니라 **"덜 뾰족한 쪽"**이 손잡이다
   - ⚠ **칼끝도 얇다.** '얇은 쪽=손잡이'로 하면 칼날을 잡고 거꾸로 든다 (실제로 그랬다)
   - 판정식 : `맨끝 굵기 / 안쪽 3칸 평균` 이 큰 쪽이 손잡이 (손잡이는 굵기가 일정 + 폼멜)
4. 쥐는 점 = 손잡이 끝에서 길이의 **0.263**(검 계열) 또는 **0.409**(장병기) — 레퍼런스 실측값
5. 양 끝 두께가 비슷하고 뭉툭하면 손잡이 없는 물건 → **무게중심**을 쥔다

출력 : `art/wep_grip.json` (67종). **무기를 새로 넣으면 좌표가 저절로 나온다.**

## 2-4. 사람이 정하는 것은 '계열' 한 단어뿐

`game.html` 의 `WEP_CLASS` : 무기 48종 → 계열 7종 (small/blade/heavy/blunt/pole/bow/item)
`WEP_RULE` : 계열 → `[기울기(수직 기준 라디안), 크기배수, 손높이]`

`wepGrip()` 은 이제 **두 좌표계를 겹치기만 한다** :
```
그림이 알려 준 자루 방향(A.ang)  →  자세가 요구하는 방향(POSE_ANG + 계열 기울기)
```

## 2-5. 앞손(hand_front)

**딴 손 그림을 얹지 않는다.** 캐릭터 그림에 이미 그려진 **주먹 사각형만 무기 위에 다시 그린다.**
(`drawHandOver` → `HERO_GRIP[key].fx0/fx1/fy0/fy1` 로 클립하고 몸을 한 번 더 그린다)
화풍·색·크기가 태생부터 맞는다. Spine 의 hand_front 슬롯을 그림 자체에서 뽑는 셈이다.

⚠ **가로 폭을 주먹보다 넓히면 꼬리까지 다시 그려져 무기를 가린다.** (실제로 가렸다)

---

# 3. 그림 제작 파이프라인 (AI 생성)

## 3-1. 경로 : Gemini → 클립보드 → 컨테이너

크롬이 gemini.google.com 다운로드를 막아서 **클립보드 우회**를 쓴다.

```
① 크롬(사장님 PC)에서 Gemini 로 시트 생성
② 이미지의 [이미지 복사] 버튼 클릭
③ powershell -ExecutionPolicy Bypass -File "$env:USERPROFILE\Downloads\clipsave.ps1" <이름>
   → C:\Users\sun\Downloads\<이름>.png 로 저장
④ device_stage_files 로 컨테이너에 올림
⑤ 슬라이서로 잘라 art/ 에 저장
⑥ python3 build.py
```

`clipsave.ps1` 은 이미 사장님 PC의 Downloads 에 있다.

### 브라우저 조작 요령
- 새 대화 탭을 만들고 `find` 로 입력창 ref 를 잡아 `type` → 전송 버튼 클릭
- 전송 후 **40초** 기다린 뒤 아래로 스크롤 → 이미지 위에 hover → 좌표 **(1089, 293)** 이 복사 버튼
- 탭이 길어지면 렌더러가 멈춘다. **새 탭으로 옮길 것**

## 3-2. 슬라이서

| 파일 | 용도 | 특징 |
|---|---|---|
| `slice_mons.py` | 몬스터·재료·소품 시트 (가로 N칸 / 2줄 격자) | 아래 두 가지 자동 처리 |
| `slice_hero.py` | 갑옷 전신 (마젠타 봉) | 봉 지우기 + 관통 자리 메우기 + 바닥그림자 제거 |
| `slice_pose.py` | 계열별 자세 (기울어진 봉) | 봉 축에 **수직 방향**으로 메운다 |
| `slice_views.py` | 방향별 그림 (뒷모습·옆모습) | 연결성분 필터 |
| `slice_paw.py` / `crop_paw.py` | 손만 (지금은 안 씀) | |
| `defringe.py` | 글로우 때문에 남은 흰 테두리 제거 | |

### 슬라이서가 자동으로 잡는 함정 (직접 겪은 것)
1. **AI가 그림 밑에 이름표·hex 코드를 써 넣는다.** 말로는 못 막는다.
   → 칸 안에서 **'잉크가 가장 많은 세로 덩어리'만 남긴다** (글자 줄은 얇아서 진다)
2. **균등 분할 시 옆 칸 생물의 끄트머리가 딸려 온다.**
   → **연결성분 중 가장 큰 것만** 남긴다
3. **봉을 지우면 꼬리에 세로 슬롯이 남는다.**
   → 구멍 **양옆 3px 안쪽**에서 색을 떠서 메운다 (가장자리를 퍼오면 창백한 띠가 남는다)
4. **AI가 "no ground shadow" 라고 해도 바닥 그림자를 그린다.** → 발밑 18% 의 거의 흰 화소 제거

## 3-3. 프롬프트 규칙 (실패에서 얻은 것)

- **"실루엣만으로 서로 구분되게"** 를 반드시 넣는다. 없으면 AI가 **넷을 전부 같은 동글이로** 그린다
- 종류마다 **'한눈에 구별되는 특징'** 을 적는다 (`monname.py` 의 `FEATURE` 사전)
- 화풍 문장은 **모든 시트에서 글자 하나 안 바꾼다**. 바꾸면 시트끼리 그림체가 갈린다
- 8마리(2줄×4칸)씩이 왕복 대비 효율이 가장 좋다
- 한글 이름 → 영어 설명은 `monname.py` 가 한다 (148낱말 수동 번역, 미번역 0)

## 3-4. 검사기

- `audit_mon.py` — 몬스터 그림 240장 슬라이스 결함 전수 검사 (가로·세로 덩어리 수 + 채움비)
- `/tmp/t_audit3.js` — 무기 48종 기하 감사 (얼굴 가림 % · 몸통 겹침 % · 손 접촉 · 잘림)

---

# 4. 현재 상태

## 4-1. 완료

| 항목 | 상태 |
|---|---|
| 몬스터 AI 그림 | **240 / 240**, 슬라이스 결함 0 |
| 재료 AI 그림 | **72 / 72** |
| 소품 AI 그림 | **33 / 33** (`PROP_ART` 전부 연결) |
| 무기 그림 | 48종류 전부 고유 (`eq_*` 67장) |
| 무기 손잡이 자동 검출 | 67종 (`gripsolve.py`) |
| 갑옷 6종 '무기 쥔 자세' | 재생성 완료 (마젠타 봉) |
| 뒷모습 | 맨몸 + 갑옷 6종. 위로 걸으면 등을 보임 (계측 30/30 프레임) |
| 스킬·조작 버튼 | AI 그림 + 금속 테두리 + 원형 쿨다운. 공격 20.0mm (NN/g 6~8세 기준 충족) |
| 상태창 | 레퍼런스 실측 2열 배치. 넘침 0 |
| 몬스터 생성 수 | 목표 수만큼만 생성 (413마리 폭주 버그 해결) |

## 4-2. 검증 결과 (최신)

```
회귀 테스트   25 / 25
NaN·예외      0
갤럭시 9기종   UI 벗어남 0 · 시트 넘침 0
  (CPU 조임 : 보급형 6배 · 플래그십 3배 · 구형 7배)
  갤A16 20~28fps · 갤S24 46~55 · 갤A54 27~32 · 갤탭A9+ 19~25 · 갤S8 22~25
무기 48종 감사  얼굴 가림 0% · 잘림 0
```

## 4-3. 남은 작업 (우선순위)

### A. 원리 ④ — 무기 계열별 자세 (가장 중요)
지금 자세는 **주먹이 볼에 붙어** 있다. 그래서 장병기(창·낫·지팡이·빗자루·별지팡이 등 14종)가
**얼굴을 지나간다.** 감사기가 지적한다.

레퍼런스(사장님이 준 상용 스프라이트 시트 13종)는 **팔이 전부 몸 밖으로 뻗어** 있다.

**해야 할 것** : 계열별 자세 그림을 만들어 붙인다 (한손 / 양손 / 장병기 / 활 / 소품).
- 자세 3종 시트는 이미 뽑았으나 **봉에 어두운 외곽선이 그려져** 검은 대각선이 남아 폐기했다
- 다시 뽑을 때 **"봉은 외곽선 없이 납작한 마젠타만"** 을 반드시 넣을 것
- 던파 조사 결론 : 쿼터뷰는 방향 수만큼 물량이 곱해지므로 **계열을 3~4개로 줄이는 게 안전**

### B. 방향별 그리는 순서 표
아래를 볼 땐 무기가 몸 앞, **위를 볼 땐 무기가 몸 뒤**여야 한다.
던파는 횡스크롤이라 이 경우가 없어 참고할 자료가 없다 — **우리가 정의해야 한다.**

### C. 머리 장비(투구) 표시
지금 **꺼져 있다** (`HEAD_OVERLAY=0`). 이유 : `hw_*` 그림이 **얼굴 구멍이 없는 아이콘**이라
겹치면 눈·코·입이 사라진다. 규격에 맞는 머리 레이어를 새로 그려야 켤 수 있다.

### D. UI 화면 4개 (가방·펫·도감·지도) 정비
### E. 펫 시스템 강화
### F. 구글 플레이 출시물
아이콘 · 스크린샷 · 짧은/긴 설명 · 연령등급 설문 · 개인정보처리방침 · 데이터 안전 양식

---

# 5. 검증 방법 (수정 후 반드시 실행)

```bash
cd /root/toriforest
python3 build.py                    # 빌드

node t_regress.js                   # 회귀 25종  → 25/25 여야 함
node t_nan.js                       # NaN·예외    → 0 이어야 함
node t_galaxy.js                    # 갤럭시 9기종 (CPU 조임) → UI벗어남 0
node t_all.js                       # 7기기 종합
node t_perf.js                      # fps

node /tmp/t_hold.js                 # 무기 48종 파지 접촉시트 → /tmp/hold48.png
node /tmp/t_audit3.js               # 무기 기하 감사 (얼굴·몸통·손접촉)
python3 audit_mon.py                # 몬스터 그림 결함 검사
```

⚠ `t_regress.js` 는 드물게 1개가 흔들린다(화면 흔들림 판정). **재실행해서 확인할 것.**

### 눈으로 보는 검증 (이게 제일 중요하다)
숫자가 통과해도 **반드시 렌더해서 본다.** 감사기가 놓친 결함을 여러 번 눈으로 잡았다.
- 무기 48종 : `/tmp/hold48.png`
- 갑옷 6종 착용샷 : `/tmp/t_armhand.js`
- 인게임 : `/tmp/t_armgame.js`

---

# 6. 지금까지 저지른 실수 (같은 실수를 반복하지 말 것)

| 실수 | 진짜 원인 | 교훈 |
|---|---|---|
| 무기가 손 앞에 뜨거나 뒤로 숨음 | 주인공 주먹이 **구멍 없는 '치는 주먹'** | 그림 문제를 숫자로 풀려 했다 |
| 가슴에 크림색 덩어리 | AI 손 그림에 **손바닥·손목**까지 붙어 있었다 | 손가락만 남겨야 한다 |
| 갑옷 끼면 손이 가슴팍에 뜸 | **갑옷 그림 6종이 두 팔을 내린 자세**였다 | 자세가 다르면 좌표가 의미 없다 |
| 칼날이 아래로 향함 | '얇은 쪽=손잡이' 규칙 — **칼끝도 얇다** | 뾰족함으로 판정해야 한다 |
| 투구가 얼굴을 덮음 | `hw_hood` 가 **얼굴 구멍 없는 아이콘** | 겹칠 그림은 같은 배치로 그려져야 한다 |
| 아이템이 칸의 1/4만 차지 | `artCanvas`(ART_FIT 0.72, 발밑 정렬)는 **땅에 선 몬스터용** | 아이콘은 틀을 꽉 채워야 한다 |
| 갑옷 6종이 전부 갈색 | 틴트 캐시 열쇠에 **손 그림 이름이 빠짐** | 캐시 키를 의심할 것 |
| 몬스터 413마리 폭주 | 쿼터를 채우면 보스 플래그가 켜져 무한 리젠 | |
| 무기 그림이 안 바뀜 | `EQ_ART_MAP` 에 **중복 키** — 뒤의 것이 이김 | |
| `element.style.background` 로 금속 테두리가 뭉개짐 | 단축 속성이 **background-clip 을 초기화**한다 | 직후에 다시 지정 |

---

# 7. 코드에서 알아둘 것

## 좌표계
- 주인공 그림은 192px 정사각. **발밑 기준선 `BASE_F = 112/120 = 0.9333`**
- 그리기 : `drawImage(spr, ox - sz*0.5, oy - sz*BASE_F, sz, sz)`
- 손 좌표는 이 기준의 **오프셋** : `fx = ox + sz*hx`, `fy = oy + sz*hy`

## 주요 전역
| 이름 | 뜻 |
|---|---|
| `HERO_GRIP` | 갑옷별 쥐는 점 · 주먹 사각형 (마젠타 봉에서 뽑음) |
| `WEP_AUTO` | 무기별 손잡이 좌표·자루 각도 (`gripsolve.py` 결과) |
| `WEP_CLASS` / `WEP_RULE` | 무기 → 계열 / 계열 → 기울기·크기 |
| `POSE_ANG` | 주인공 자세의 자루 방향 (지금 -1.5708 = 수직) |
| `PROP_ART` | 소품 종류 → 그림 파일 |
| `HEAD_OVERLAY` | 투구 겹치기 on/off (지금 0) |
| `_DIRSPR` | 방향별 그림 캐시 (뒷모습) |

## 그림 로딩
`artLoad()` 는 **첫 화면에 필요한 것만 기다린다.** 몬스터 240 · 재료 72 는 뒤에서 받는다.
늦게 도착하면 해당 캐시만 버려서 다음에 그릴 때 그림 판이 쓰이게 한다
(`artIsCritical` / `artLateArrive`). 이걸 안 하면 보급형 폰 부팅이 6~7초다.

---

# 8. 프로젝트 문서 (claude.ai 프로젝트에 저장돼 있음)

- `도토리숲-현재상태.md`
- `도토리숲-무기파지-실측.md` — 레퍼런스 실측값 (검 길이/키 0.466 · 자루 0.263 · 각도 +30.1° 등)
- `도토리숲-그림파이프라인-버튼규격.md` — 터치 크기 기준(NN/g 6~8세 20mm 등)
- `던전앤파이터-방식-조사.md` — DNF 1차 자료 전수 조사 (출처 전부 포함)

---

# 9. 사장님 PC 환경

- Windows, `desktop-f3pcj6k`
- 연결 폴더 : `C:\Users\sun\Downloads`
- `clipsave.ps1` 이 Downloads 에 있다 (클립보드 → PNG)
- **게임 소스와 그림은 사장님 PC에 없다.** 전부 작업 컨테이너 `/root/toriforest` 안에 있다
  (`C:\Users\sun\toriart` 는 `recv.py` 하나에 `sheets` 폴더는 비어 있음)

---

# 10. ⚠ 인수인계 꾸러미 (이걸 새 계정에 올려야 이어서 할 수 있다)

**작업 컨테이너는 세션이 끝나면 사라진다.** `/root/toriforest` 는 새 계정에서 볼 수 없다.
그래서 아래 파일을 사장님 PC로 내려보냈다.

```
도토리숲_인수인계.zip   (C:\Users\sun\Downloads)
├─ HANDOVER.md                이 문서
├─ toriforest/
│   ├─ game.html              ★ 단일 원본 (1.26MB)
│   ├─ build.py               빌드 (art 를 base64 로 주입 → dotorisup.html)
│   ├─ art/                   AI 그림 전부 (522개 : 몬스터240·재료72·소품33·무기67·주인공·UI)
│   │   ├─ wep_grip.json      무기 67종 손잡이 좌표 (gripsolve.py 결과)
│   │   └─ hero_grip.json     갑옷 7종 쥐는 점·주먹 사각형 (마젠타 봉 결과)
│   ├─ icons/                 앱 아이콘
│   ├─ *.py                   슬라이서·검출기·프롬프트 생성기 전부
│   ├─ *.js                   엔진 조각·테스트
│   └─ tests/                 t_*.js (회귀·갤럭시·감사)
└─ apk/                       안드로이드 빌드 (AndroidManifest·build.sh·서명키 tori.jks)
```

## 새 계정에서 이어받는 순서

1. 이 zip 을 새 대화에 올린다 (또는 연결 폴더에 두고 스테이징한다)
2. `unzip 도토리숲_인수인계.zip -d /root/` → `/root/toriforest`, `/root/apk` 로 푼다
3. `HANDOVER.md` 의 **0. 절대규칙**을 먼저 읽는다
4. `cd /root/toriforest && python3 build.py` 로 빌드가 되는지부터 확인한다
5. `node tests/t_regress.js` → **25/25** 나오는지 확인한 뒤에 손댄다
   (테스트가 `/tmp/t_*.js` 를 찾으면 `cp tests/t_*.js /tmp/` 해 준다)
6. `4-3 남은 작업` 의 **A(무기 계열별 자세)** 부터 시작한다

**포함하지 않은 것** : git 이력(236MB), 스크린샷 폴더, 빌드 산출물(dotorisup.html·apk).
전부 `build.py` / `apk/build.sh` 로 다시 만들 수 있다. **원본과 그림은 하나도 안 빠졌다.**
