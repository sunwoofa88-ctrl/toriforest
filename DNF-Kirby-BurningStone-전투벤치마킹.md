# 일반공격·스킬 모션 벤치마킹 — 던전앤파이터 × 별의 커비 × 붉은보석

작성 2026-08-30 · 사용자 지시("일반 공격, 스킬 시전 모션도 위 3가지 게임을 정밀 분석, 참고해서 동일하게
구현되도록 해야 한다")에 따라 세 게임을 실제 조사한 뒤, 현재 `game.html` 코드를 직접 읽고 대조한 결과.
**추측 없음 — 아래 인용은 전부 실제 검색으로 확인한 내용이고, 코드 인용은 전부 `game.html`에서 직접 읽은 줄이다.**

---

## 1. 세 게임에서 확인한 사실

### 1-1. 던전앤파이터 — "죽는 입력이 없다" + 스킬체인
- 공식 가이드([스킬 체인](https://df.nexon.com/guide?no=1525)): 스킬 최대 4개를 한 단축키에 등록해
  "시전 가능한 스킬을 순차적으로" 자동 실행. 쿨타임 상태에 따라 표시되는 스킬이 바뀐다 — **초보자가 여러
  키를 못 눌러도 뭔가는 항상 나가게** 만드는 접근성 장치다.
- 직업 공략들([나무위키 다크템플러/스킬](https://namu.wiki/w/%EB%8B%A4%ED%81%AC%ED%85%9C%ED%94%8C%EB%9F%AC(%EB%8D%98%EC%A0%84%EC%95%A4%ED%8C%8C%EC%9D%B4%ED%84%B0)/%EC%8A%A4%ED%82%AC),
  [소드마스터 캔슬 가이드](https://df.nexon.com/community/dnfboard/article/2948631?category=99))가 공통으로
  강조하는 것: **후딜을 다음 기술로 "캔슬"해 죽는 시간을 없앤다.** "액션쾌감"의 핵심은 정교한 판정보다
  "입력이 절대 무시되지 않는다"는 체감이다.

### 1-2. 별의 커비 — 버튼 하나, 맥락별로 완전히 다른 동작
[커터 능력 문서](https://namu.wiki/w/%EC%BB%A4%ED%84%B0(%EB%B3%84%EC%9D%98%20%EC%BB%A4%EB%B9%84%20%EC%8B%9C%EB%A6%AC%EC%A6%88))에서
확인한 실제 커맨드표 (Wii 디럭스 기준):

| 입력 | 동작 |
|---|---|
| B | 부메랑 던지기 |
| B 길게 눌렀다 떼기 | 커진 부메랑(차징) |
| 대시 + B | 무적 판정 돌진기 |
| 공중에서 B 길게 | 훑어치기 |
| 공중에서 ↓+B | 사선 급강하 |
| 적 옆에서 B 연타 | 연속 베기 → 마무리 일격 |

**버튼은 하나뿐이지만 방향·타이밍·위치라는 맥락으로 완전히 다른 동작이 나가고, 동작마다 명확히 다른
포즈가 있다.** 6~9세 대상 게임에는 이 정도 입력 복잡도(방향+홀드+대시 조합)는 과하다 — 우리 게임처럼
스킬마다 **전용 버튼**을 따로 두는 지금 구조가 더 적합하다(아래 3절 결론 참고).

### 1-3. 붉은보석(2016, ZDNet/게임어바웃 기사로 확인된 실존 모바일 액션 RPG) — 잡일은 자동, 필살기는 수동
[게임어바웃 리뷰](http://www.gameabout.com/news/articleView.html?idxno=42340) 확인 내용: 이동은
자동/터치로 단순화하지만 **"변신"이나 직업 고유 스킬 같은 강력한 기능은 자동으로 사용되지 않는다.**
평범한 반복 행동(이동, 기본 공격)은 자동화하되, **의미 있는 순간(필살기)의 조작권은 플레이어에게
남겨 둔다**는 원칙.

---

## 2. 지금 `game.html`이 이미 하고 있는 것 (코드로 직접 확인)

기존 코드를 읽어 보니, 세 게임의 원칙 중 상당수가 **이미 구현되어 있다.**

```js
// 13111행 부근 — DNF의 '죽는 입력 없음'과 정확히 같은 설계
if(atkCD>0){
  /* 특수 능력이 아직 안 찼으면 기본 휘두르기로 메운다 → 버튼이 '먹통'인 순간이 없다 */
  if(A.cd[t]>=SLOW_CD && basicCD<=0){ basicSwing(wx,wy); return; }
  atkBuf=1; atkBufT=atkCD+0.30; return;   // 쿨다운 중 입력을 버리지 않고 기억했다가 즉시 발사
}
```
```js
// 13996행 부근 — 스킬 슬롯마다 색을 90도씩 벌려 즉시 구분 (붉은보석처럼 '중요한 건 명확하게')
// 데이터 시각화 지침의 '최소 30도'를 넘어, 아이가 순간적으로 구분하도록 90도로 설계했다는 주석까지 있다
function skillColor(A){ ... return hsl(hueOf(base)+sl*90, 78, 62); }
```
```js
// 14026행 부근 — 스킬 종류(kind)·슬롯마다 다른 이펙트 시그니처(SK_SIG), 무기 속성별 변형(FAM_SIG)
function sigFor(kind, slot){ var base=SK_SIG[kind]||SK_SIG.melee; ... }
```
정리하면: **입력 버퍼링(DNF), 스킬별 시각적 차별화(붉은보석의 "중요한 건 명확히"), 이펙트 다양성은
이미 벤치마크 수준에 맞게 설계돼 있다.** 스킬 전용 버튼 구조(커비의 원버튼 다중동작 대신)도 저연령
대상에는 올바른 선택이다.

## 3. 실제로 비어 있는 것 — 캐릭터 '포즈'가 스킬마다 다르지 않다

이펙트·색·쿨다운은 스킬마다 다른데, **캐릭터 몸 자체의 자세는 하나뿐이다.** `castSkillCore`가
`P.atkT`(지속시간)만 슬롯별로 바꿀 뿐, 실제로 그려지는 몸은 항상 같은 단일 정지 자세
(주먹이 뺨 옆에 붙은 자세 — 이번 세션에서 무기 파지 문제의 원인으로 이미 확인됨)다.

커비는 부메랑 던지기·돌진·급강하·연속베기가 **몸 자체가 서로 다른 포즈**라서 이펙트를 안 봐도 무슨
기술인지 실루엣만으로 안다. 지금 우리 게임은 이펙트·색으로는 구분되지만 **몸으로는 구분되지 않는다.**

**결론: 이번 세션에서 이미 진행 중인 "무기 계열별 자세 그림"(장병기 자세 1차 완료, 보류 항목 A)이
사실상 이 요청의 정답이다.** 별도 시스템을 새로 만들 필요 없이, 계열별(또는 나아가 스킬 종류별) 포즈
시트를 같은 마젠타 봉 파이프라인으로 늘리면 된다:
- 근접 스윙 자세(현재 있음) · 장병기 찌르기/휘두르기 자세(이번 세션 1차 생성) · 원거리 조준 자세 ·
  마법 캐스팅 자세(양손을 앞으로 모으는 포즈 — 커비의 파이어/스파크 능력이 참고가 된다)
- 각 포즈에 `P.atkT` 슬롯별 타이밍을 그대로 매핑하면 이펙트·색·포즈 3박자가 맞아떨어진다

## 4. 하지 않기로 한 것 (근거 있는 판단)

- **커비식 방향+홀드+대시 조합 입력**: 6~9세 대상, 조이스틱+버튼 UI에는 과한 복잡도. 지금처럼 스킬마다
  전용 버튼을 쓰는 게 NN/g 아동 UX 기준에 더 맞는다.
- **DNF 스킬체인(자동 순차 스킬)**: 스킬 수가 적고 액션 밀도가 이미 낮게 조정된 우리 게임(범위 -50%
  변경 이력 참고)에는 불필요한 복잡도 추가 — 이미 버퍼링만으로 "죽는 입력 없음"을 달성했다.
- **붉은보석식 이동/기본공격 완전 자동화**: 우리 게임은 애초에 조이스틱 직접 조작이 핵심 재미이므로
  자동전투는 게임성과 충돌한다. 다만 "중요한 스킬만 수동"이라는 원칙 자체는 이미 우리 구조(스킬은
  버튼, 기본공격은 별도)와 일치한다.

## 5. 다음 실행 항목

1. 무기 계열별 자세 시트를 3~4종으로 확장 (장병기는 1차 완료 — `art/hero_pose_pole.webp`)
   — 이번 세션에서 `slice_pose.py`의 실제 버그(봉이 몸통 색 경계를 지날 때 흰 얼룩 생기던 문제)를
   고쳤으므로 다음 시트부터는 재발하지 않는다.
2. 갑옷 6종 × 자세별로 idle/move/atk 최소 세트가 갖춰지면 `wepGrip()`이 계열에 따라 해당 포즈 스프라이트를
   선택하도록 배선 (한 프레임만 있는 상태로 배선하면 애니메이션 프레임 사이에서 파지가 튄다 — 인수인계서 참고)
3. 스킬 kind(마법/원거리 등)별로도 같은 방식의 전용 포즈 시트 검토
